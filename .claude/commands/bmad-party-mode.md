Load and execute the BMad skill located at `_bmad/core/bmad-party-mode/SKILL.md`.

First run bmad-init to load configuration:
```
python _bmad/core/bmad-init/scripts/bmad_init.py load --project-root .
```

Then follow all instructions in the skill file.
