---
phase: 06-inner-loop-v1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/engine/src/aerocloud/optimizer/__init__.py
  - packages/engine/src/aerocloud/optimizer/loss.py
  - packages/engine/src/aerocloud/optimizer/convergence.py
  - packages/engine/src/aerocloud/models/optimizer.py
  - packages/engine/tests/optimizer/__init__.py
  - packages/engine/tests/optimizer/conftest.py
  - packages/engine/tests/optimizer/unit/__init__.py
  - packages/engine/tests/optimizer/unit/test_loss_wmse.py
  - packages/engine/tests/optimizer/unit/test_loss_overlap.py
  - packages/engine/tests/optimizer/unit/test_loss_fidelity.py
  - packages/engine/tests/optimizer/unit/test_loss_temporal.py
  - packages/engine/tests/optimizer/unit/test_loss_total.py
  - packages/engine/tests/optimizer/unit/test_convergence.py
  - packages/engine/tests/optimizer/unit/test_grad_clipping.py
autonomous: true
requirements:
  - INNER-01
  - INNER-02
  - INNER-03
  - INNER-04
  - INNER-05
  - INNER-07
  - INNER-09

must_haves:
  truths:
    - "L_wmse penalizes uncovered interior pixels weighted by SDF depth"
    - "L_overlap detects sprite overlaps via additive density exceeding 1.0"
    - "L_fidelity prevents artificial scale inflation using cosine similarity"
    - "L_temporal penalizes position drift (default weight 0.0)"
    - "L_total combines all four terms with configurable weights"
    - "Convergence detector terminates on loss plateau"
    - "Gradient clipping limits param norm to 1.0"
  artifacts:
    - path: "packages/engine/src/aerocloud/optimizer/loss.py"
      provides: "4 loss functions + compute_total_loss + compute_additive_density"
      exports: ["compute_l_wmse", "compute_l_overlap", "compute_l_fidelity", "compute_l_temporal", "compute_total_loss", "compute_additive_density"]
    - path: "packages/engine/src/aerocloud/optimizer/convergence.py"
      provides: "Rolling-window plateau detection"
      exports: ["check_convergence"]
    - path: "packages/engine/src/aerocloud/models/optimizer.py"
      provides: "LossWeights and OptimizationResult Pydantic models"
      exports: ["LossWeights", "OptimizationResult", "InnerLoopConfig"]
    - path: "packages/engine/tests/optimizer/unit/test_loss_wmse.py"
      provides: "L_wmse unit tests"
    - path: "packages/engine/tests/optimizer/unit/test_loss_overlap.py"
      provides: "L_overlap unit tests"
  key_links:
    - from: "packages/engine/src/aerocloud/optimizer/loss.py"
      to: "packages/engine/src/aerocloud/renderer/_renderer.py"
      via: "compute_additive_density accesses renderer._sprites and renderer.params"
      pattern: "renderer\\._sprites|renderer\\.params"
    - from: "packages/engine/src/aerocloud/models/optimizer.py"
      to: "packages/engine/src/aerocloud/models/base.py"
      via: "AeroCloudBase inheritance"
      pattern: "class LossWeights\\(AeroCloudBase\\)"
---

<objective>
Scaffolding, Pydantic models, all 4 loss functions, convergence detection, and their unit tests.

Purpose: Establish the mathematical foundation of the inner loop -- each loss function is independently unit-testable. The additive compositing helper for L_overlap is the hardest constraint (accessing renderer internals) and must be validated early.

Output: `optimizer/` package with loss.py, convergence.py, models, and 7+ unit test files with >= 18 tests.
</objective>

<execution_context>
@/var/www/wordcloud-app-v2/server/aerocloud-engine/.claude/get-shit-done/workflows/execute-plan.md
@/var/www/wordcloud-app-v2/server/aerocloud-engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inner-loop-v1/06-CONTEXT.md
@.planning/phases/06-inner-loop-v1/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/engine/src/aerocloud/renderer/_renderer.py:
```python
class DifferentiableRenderer(nn.Module):
    def __init__(self, params_n4: torch.Tensor, sprites: list[torch.Tensor], device: torch.device) -> None:
        self.params = nn.Parameter(params_n4.to(device, dtype=torch.float32))  # (N, 4) [y, x, scale, rotation]
        self._sprites: list[torch.Tensor] = [s.to(device, dtype=torch.float32) for s in sprites]
        self._device = device

    def forward(self, canvas_h: int, canvas_w: int) -> torch.Tensor:
        # Returns (1, 1, canvas_h, canvas_w) density in [0, 1] via alpha-over
        # Uses: F.affine_grid + F.grid_sample per sprite
        # Rotation clamped via torch.remainder
        # Scale soft-clamped via softplus(s_i - 0.01) + 0.01
```

From packages/engine/src/aerocloud/models/base.py:
```python
class AeroCloudBase(BaseModel):
    model_config = ConfigDict(frozen=True, extra="forbid", strict=True)
```

From packages/engine/src/aerocloud/geometry/sdf.py:
```python
def compute_sdf(mask: np.ndarray) -> np.ndarray:
    # Returns float32 (H, W) array. positive=inside, negative=outside.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pydantic models + loss functions + additive compositing</name>
  <files>
    packages/engine/src/aerocloud/models/optimizer.py
    packages/engine/src/aerocloud/optimizer/__init__.py
    packages/engine/src/aerocloud/optimizer/loss.py
    packages/engine/src/aerocloud/optimizer/convergence.py
    packages/engine/tests/optimizer/__init__.py
    packages/engine/tests/optimizer/conftest.py
    packages/engine/tests/optimizer/unit/__init__.py
    packages/engine/tests/optimizer/unit/test_loss_wmse.py
    packages/engine/tests/optimizer/unit/test_loss_overlap.py
    packages/engine/tests/optimizer/unit/test_loss_fidelity.py
    packages/engine/tests/optimizer/unit/test_loss_temporal.py
    packages/engine/tests/optimizer/unit/test_loss_total.py
    packages/engine/tests/optimizer/unit/test_convergence.py
    packages/engine/tests/optimizer/unit/test_grad_clipping.py
  </files>
  <read_first>
    packages/engine/src/aerocloud/renderer/_renderer.py
    packages/engine/src/aerocloud/models/base.py
    packages/engine/src/aerocloud/renderer/__init__.py
    packages/engine/tests/renderer/conftest.py
  </read_first>
  <behavior>
    --- Pydantic models (models/optimizer.py) ---
    - LossWeights(AeroCloudBase): alpha=1.0, beta=10.0, gamma=0.1, lambda_=0.0 (all ge=0.0, frozen=True)
    - InnerLoopConfig(AeroCloudBase): weights: LossWeights, lr: float=0.001, max_epochs: int=100, min_epochs_before_convergence: int=20, convergence_window: int=10, convergence_epsilon: float=0.001, stage_resolutions: list[int]=[8,32,128]
    - OptimizationResult(AeroCloudBase with strict=False, arbitrary_types_allowed=True): params: torch.Tensor, stage_loss_histories: list[list[float]], total_epochs: int(ge=0), convergence_flags: list[bool], wall_clock_s: float(ge=0.0)

    --- Loss functions (optimizer/loss.py) per D-01..D-06 ---
    - compute_l_wmse(density (1,1,H,W), sdf (1,1,H,W)) -> scalar: mean((clamp(sdf,min=0) * (1-density))^2). Test: circle SDF with full coverage -> ~0.0; empty density -> positive value; outside pixels (sdf<0) contribute 0.
    - compute_l_overlap(additive_density (1,1,H,W)) -> scalar: mean(ReLU(density-1.0)^2). Test: density=0.5 everywhere -> 0.0; density=2.0 at one pixel -> positive value.
    - compute_l_fidelity(s_ref (N,), params (N,4)) -> scalar: 1.0 - cos_sim(s_ref, params[:,2]). Test: identical scales -> 0.0; orthogonal -> 1.0; value in [0, 2].
    - compute_l_temporal(params_current (N,4), params_initial (N,4)) -> scalar: mean((current[:,:2] - initial[:,:2])^2). Test: same positions -> 0.0; shifted -> positive.
    - compute_total_loss(weights, l_wmse, l_overlap, l_fidelity, l_temporal) -> scalar: weighted sum per D-06.
    - compute_additive_density(renderer, canvas_h, canvas_w) -> (1,1,H,W): sum of warped sprites (can exceed 1.0). Test: 2 overlapping sprites -> max > 1.0.

    --- Convergence detection (optimizer/convergence.py) per D-14/D-15 ---
    - check_convergence(loss_history: list[float], window: int=10, epsilon: float=0.001) -> bool: True if (max-min)/max(abs(max),1e-8) < epsilon over last `window` entries. Test: flat history -> True; diverging -> False; short history (<window) -> False.

    --- Gradient clipping test ---
    - After backward() on a loss with large gradient, clip_grad_norm_ limits norm to 1.0. Test: create params with requires_grad, compute loss, backward, verify grad norm > 1.0 before clip, <= 1.0 after clip.
  </behavior>
  <action>
    **RED phase first.** Create all test files with failing tests before any implementation.

    1. Create `packages/engine/src/aerocloud/models/optimizer.py`:
       - `LossWeights(AeroCloudBase)`: fields alpha (default=1.0, ge=0.0), beta (default=10.0, ge=0.0), gamma (default=0.1, ge=0.0), lambda_ (default=0.0, ge=0.0). `model_config = ConfigDict(frozen=True, extra="forbid", strict=True)`.
       - `InnerLoopConfig(AeroCloudBase)`: fields weights (LossWeights, default_factory=LossWeights), lr (float, default=0.001, gt=0.0), max_epochs (int, default=100, ge=1), min_epochs_before_convergence (int, default=20, ge=0), convergence_window (int, default=10, ge=2), convergence_epsilon (float, default=0.001, gt=0.0), stage_resolutions (list[int], default=[8,32,128]).
       - `OptimizationResult(AeroCloudBase)`: Override model_config with `ConfigDict(frozen=True, extra="forbid", strict=False, arbitrary_types_allowed=True)`. Fields: params (torch.Tensor), stage_loss_histories (list[list[float]]), total_epochs (int, ge=0), convergence_flags (list[bool]), wall_clock_s (float, ge=0.0).

    2. Create `packages/engine/src/aerocloud/optimizer/__init__.py`:
       - Initially export only loss functions and models. InnerLoop will be added in Plan 02.
       - Exports: compute_l_wmse, compute_l_overlap, compute_l_fidelity, compute_l_temporal, compute_total_loss, compute_additive_density, check_convergence, LossWeights, InnerLoopConfig, OptimizationResult.

    3. Create `packages/engine/src/aerocloud/optimizer/loss.py` per D-01..D-06:
       - `compute_l_wmse(density: torch.Tensor, sdf: torch.Tensor) -> torch.Tensor`: `sdf_positive = sdf.clamp(min=0.0); return ((sdf_positive * (1.0 - density)) ** 2).mean()`.
       - `compute_l_overlap(additive_density: torch.Tensor) -> torch.Tensor`: `return (F.relu(additive_density - 1.0) ** 2).mean()`.
       - `compute_l_fidelity(s_ref: torch.Tensor, params: torch.Tensor) -> torch.Tensor`: `s_current = params[:, 2]; return 1.0 - F.cosine_similarity(s_ref.unsqueeze(0), s_current.unsqueeze(0)).squeeze()`.
       - `compute_l_temporal(params_current: torch.Tensor, params_initial: torch.Tensor) -> torch.Tensor`: `return ((params_current[:, :2] - params_initial[:, :2]) ** 2).mean()`.
       - `compute_total_loss(weights: LossWeights, l_wmse, l_overlap, l_fidelity, l_temporal) -> torch.Tensor`: `return weights.alpha * l_wmse + weights.beta * l_overlap + weights.gamma * l_fidelity + weights.lambda_ * l_temporal`.
       - `compute_additive_density(renderer: DifferentiableRenderer, canvas_h: int, canvas_w: int) -> torch.Tensor`: Replicate the renderer's per-sprite warp logic (affine_grid + grid_sample) but SUM instead of alpha-over. Access `renderer._sprites`, `renderer.params`, `renderer._device`. Apply same rotation clamping (torch.remainder) and scale softplus as the renderer forward(). Return (1,1,H,W) tensor where values can exceed 1.0.

    4. Create `packages/engine/src/aerocloud/optimizer/convergence.py` per D-14/D-15:
       - `check_convergence(loss_history: list[float], window: int = 10, epsilon: float = 0.001) -> bool`: If `len(loss_history) < window` return False. Take last `window` values. `range_val = max(window_vals) - min(window_vals)`. `scale = max(abs(max(window_vals)), 1e-8)`. Return `range_val / scale < epsilon`.

    5. Create test conftest at `packages/engine/tests/optimizer/conftest.py`:
       - `gpu` skip marker (same pattern as renderer conftest).
       - `cpu_device()` fixture returning `torch.device("cpu")`.
       - `tiny_renderer(cpu_device)` fixture: creates a DifferentiableRenderer with 2 sprites (4x4 diagonal eye patterns), params_n4 = `torch.tensor([[2.0, 2.0, 1.0, 0.0], [2.0, 2.0, 1.0, 0.0]])` (overlapping positions), device=cpu. Returns the renderer.
       - `circle_sdf_8(cpu_device)` fixture: creates a (1,1,8,8) float32 SDF for a filled circle of radius 3 centered at (4,4). Positive inside, negative outside.
       - `ref_weights_2()` fixture: `torch.tensor([1.0, 0.5])` -- 2-word reference scale vector.

    6. Create unit tests (each file has 2-4 tests per the behavior block above). Use the conftest fixtures. Every test checks a specific behavior with known inputs and exact expected outputs. Include `test_gradient_flows` tests verifying `.backward()` does not raise and `.grad is not None` for each loss function.

    Per D-23: all tests run on CPU by default.
  </action>
  <verify>
    <automated>cd /var/www/wordcloud-app-v2/server/aerocloud-engine && uv run pytest packages/engine/tests/optimizer/unit/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def compute_l_wmse" packages/engine/src/aerocloud/optimizer/loss.py
    - grep -q "def compute_l_overlap" packages/engine/src/aerocloud/optimizer/loss.py
    - grep -q "def compute_l_fidelity" packages/engine/src/aerocloud/optimizer/loss.py
    - grep -q "def compute_l_temporal" packages/engine/src/aerocloud/optimizer/loss.py
    - grep -q "def compute_total_loss" packages/engine/src/aerocloud/optimizer/loss.py
    - grep -q "def compute_additive_density" packages/engine/src/aerocloud/optimizer/loss.py
    - grep -q "def check_convergence" packages/engine/src/aerocloud/optimizer/convergence.py
    - grep -q "class LossWeights" packages/engine/src/aerocloud/models/optimizer.py
    - grep -q "class InnerLoopConfig" packages/engine/src/aerocloud/models/optimizer.py
    - grep -q "class OptimizationResult" packages/engine/src/aerocloud/models/optimizer.py
    - grep -q "arbitrary_types_allowed=True" packages/engine/src/aerocloud/models/optimizer.py
    - grep -q "alpha.*1.0" packages/engine/src/aerocloud/models/optimizer.py
    - grep -q "beta.*10.0" packages/engine/src/aerocloud/models/optimizer.py
    - grep -q "sdf.clamp(min=0.0)" packages/engine/src/aerocloud/optimizer/loss.py
    - grep -q "F.relu" packages/engine/src/aerocloud/optimizer/loss.py
    - grep -q "F.cosine_similarity" packages/engine/src/aerocloud/optimizer/loss.py
    - All unit tests pass: uv run pytest packages/engine/tests/optimizer/unit/ -x -q exits 0
    - At least 18 tests: uv run pytest packages/engine/tests/optimizer/unit/ --co -q | tail -1 shows >= 18
  </acceptance_criteria>
  <done>
    All 4 loss functions + compute_total_loss + compute_additive_density + check_convergence implemented per D-01..D-06, D-14/D-15. Pydantic models (LossWeights, InnerLoopConfig, OptimizationResult) per D-19..D-21. All unit tests green (>= 18 tests). Gradient flows verified for each loss function.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream tensors | SDF and params come from Phase 4/5 -- already validated |
| renderer internals | compute_additive_density accesses _sprites (private) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | D (Denial of Service) | optimizer loop | accept | Max 100 epochs per stage (D-11) caps compute; Phase 12 adds hard timeout |
| T-06-02 | I (Information Disclosure) | loss values | accept | Loss scalars are floats with no PII; no network exposure in Phase 6 |
| T-06-03 | T (Tampering) | renderer._sprites access | accept | Inner loop owns the renderer instance; no external access path |
</threat_model>

<verification>
- `uv run pytest packages/engine/tests/optimizer/unit/ -x -q` -- all unit tests green
- `uv run mypy packages/engine/src/aerocloud/optimizer/ --strict` -- no type errors
- `uv run ruff check packages/engine/src/aerocloud/optimizer/` -- no lint errors
</verification>

<success_criteria>
- 4 loss functions return correct scalar tensors for known inputs
- compute_additive_density produces values > 1.0 for overlapping sprites
- check_convergence correctly detects flat loss curves and rejects short/diverging ones
- Gradient flows through each loss function (backward does not raise, grad is not None)
- LossWeights defaults match D-06: alpha=1.0, beta=10.0, gamma=0.1, lambda_=0.0
- >= 18 unit tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/06-inner-loop-v1/06-01-SUMMARY.md`
</output>
