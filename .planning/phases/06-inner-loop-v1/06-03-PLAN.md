---
phase: 06-inner-loop-v1
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - packages/engine/tests/optimizer/property/__init__.py
  - packages/engine/tests/optimizer/property/test_loss_properties.py
  - packages/engine/tests/optimizer/determinism/__init__.py
  - packages/engine/tests/optimizer/determinism/test_determinism.py
  - packages/engine/tests/optimizer/memory/__init__.py
  - packages/engine/tests/optimizer/memory/test_rss.py
autonomous: true
requirements:
  - INNER-01
  - INNER-02
  - INNER-03
  - INNER-04
  - INNER-05
  - INNER-08
  - INNER-10

must_haves:
  truths:
    - "No NaN/Inf losses across 1000 random seeds (hypothesis property)"
    - "L_overlap >= 0, L_wmse >= 0, L_fidelity in [0, 2] (property bounds)"
    - "Same seed produces byte-identical loss curves over 10 runs"
    - "RSS does not grow > 50 MiB across 100 inner-loop optimization steps"
  artifacts:
    - path: "packages/engine/tests/optimizer/property/test_loss_properties.py"
      provides: "Hypothesis property tests for loss bounds and NaN-freedom"
    - path: "packages/engine/tests/optimizer/determinism/test_determinism.py"
      provides: "Determinism tests: 10 runs with same seed -> identical"
    - path: "packages/engine/tests/optimizer/memory/test_rss.py"
      provides: "RSS stability test over 100 optimization steps"
  key_links:
    - from: "packages/engine/tests/optimizer/property/test_loss_properties.py"
      to: "packages/engine/src/aerocloud/optimizer/loss.py"
      via: "Imports all loss functions"
      pattern: "compute_l_wmse|compute_l_overlap"
    - from: "packages/engine/tests/optimizer/determinism/test_determinism.py"
      to: "packages/engine/src/aerocloud/optimizer/inner_loop.py"
      via: "InnerLoop.optimize() called with set_seed"
      pattern: "InnerLoop.*optimize"
    - from: "packages/engine/tests/optimizer/memory/test_rss.py"
      to: "packages/engine/src/aerocloud/optimizer/inner_loop.py"
      via: "InnerLoop.optimize() called in loop, RSS measured"
      pattern: "psutil.*rss"
---

<objective>
Property tests, determinism tests, and memory stability tests completing the Phase 6 test pyramid to >= 35 total tests (D-22).

Purpose: Validates the mathematical correctness (no NaN/Inf), reproducibility (determinism), and operational safety (no memory leaks) of the inner loop. These are the ROADMAP success criteria 3, 4, and 5.

Output: 3 test subdirectories with >= 12 additional tests, bringing total Phase 6 tests to >= 35.
</objective>

<execution_context>
@/var/www/wordcloud-app-v2/server/aerocloud-engine/.claude/get-shit-done/workflows/execute-plan.md
@/var/www/wordcloud-app-v2/server/aerocloud-engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-inner-loop-v1/06-CONTEXT.md
@.planning/phases/06-inner-loop-v1/06-RESEARCH.md
@.planning/phases/06-inner-loop-v1/06-01-SUMMARY.md
@.planning/phases/06-inner-loop-v1/06-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01 and 02 -->

From packages/engine/src/aerocloud/optimizer/loss.py:
```python
def compute_l_wmse(density: torch.Tensor, sdf: torch.Tensor) -> torch.Tensor: ...
def compute_l_overlap(additive_density: torch.Tensor) -> torch.Tensor: ...
def compute_l_fidelity(s_ref: torch.Tensor, params: torch.Tensor) -> torch.Tensor: ...
def compute_l_temporal(params_current: torch.Tensor, params_initial: torch.Tensor) -> torch.Tensor: ...
def compute_total_loss(weights: LossWeights, ...) -> torch.Tensor: ...
def compute_additive_density(renderer: DifferentiableRenderer, canvas_h: int, canvas_w: int) -> torch.Tensor: ...
```

From packages/engine/src/aerocloud/optimizer/inner_loop.py:
```python
class InnerLoop:
    def __init__(self, renderer, sdf, ref_weights, config=None): ...
    def optimize(self) -> OptimizationResult: ...
```

From packages/engine/src/aerocloud/utils/determinism.py:
```python
def set_seed(seed: int) -> None: ...
```

From packages/engine/tests/renderer/memory/test_rss.py (pattern):
```python
import psutil, os
process = psutil.Process(os.getpid())
rss_before = process.memory_info().rss
# ... run iterations ...
delta_mib = (rss_after - rss_before) / 1024 / 1024
assert delta_mib < 50.0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Property tests (hypothesis) for loss function bounds</name>
  <files>
    packages/engine/tests/optimizer/property/__init__.py
    packages/engine/tests/optimizer/property/test_loss_properties.py
  </files>
  <read_first>
    packages/engine/src/aerocloud/optimizer/loss.py
    packages/engine/src/aerocloud/models/optimizer.py
    packages/engine/tests/optimizer/conftest.py
    packages/engine/tests/renderer/property/test_renderer_properties.py
  </read_first>
  <action>
    Create `packages/engine/tests/optimizer/property/__init__.py` (empty).

    Create `packages/engine/tests/optimizer/property/test_loss_properties.py` with hypothesis property tests:

    1. **test_l_wmse_is_finite** (hypothesis, max_examples=200): Generate random density (1,1,H,W) with H,W in [4,32] and random SDF. Assert result `torch.isfinite`. Also assert result >= 0.

    2. **test_l_overlap_non_negative** (hypothesis, max_examples=200): Generate random additive_density. Assert compute_l_overlap >= 0. Assert torch.isfinite.

    3. **test_l_fidelity_in_range** (hypothesis, max_examples=200): Generate random s_ref (N,) and params (N,4) with N in [1,20]. Assert 0.0 <= result <= 2.0 (cosine similarity range). Assert torch.isfinite. Handle the edge case: if both vectors are zero-norm, F.cosine_similarity returns 0.0 (due to eps), so result is 1.0 -- still in [0, 2].

    4. **test_l_temporal_non_negative** (hypothesis, max_examples=200): Generate random params_current and params_initial (N,4). Assert result >= 0. Assert torch.isfinite.

    5. **test_total_loss_is_finite** (hypothesis, max_examples=100): Generate random inputs for all 4 loss terms. Compute total loss with default LossWeights. Assert torch.isfinite.

    6. **test_random_params_produce_finite_loss_through_renderer** (max_examples=50): Create a tiny DifferentiableRenderer with random params (N in [1,5]), run forward + compute_additive_density + all losses + total. Assert all finite.

    7. **test_l_overlap_zero_when_no_overlap**: Generate density in [0, 1] range (no overlaps). Assert compute_l_overlap == 0.0 exactly.

    Use `@settings(suppress_health_check=[HealthCheck.too_slow])` on all tests. Use `st.floats(min_value=-10, max_value=10, allow_nan=False, allow_infinity=False)` for tensor values. Build tensors via `torch.tensor(draw(st.lists(...)))`.

    D-23: all tests CPU-only. No `@gpu` marker.
  </action>
  <verify>
    <automated>cd /var/www/wordcloud-app-v2/server/aerocloud-engine && uv run pytest packages/engine/tests/optimizer/property/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "hypothesis" packages/engine/tests/optimizer/property/test_loss_properties.py
    - grep -q "test_l_wmse_is_finite" packages/engine/tests/optimizer/property/test_loss_properties.py
    - grep -q "test_l_overlap_non_negative" packages/engine/tests/optimizer/property/test_loss_properties.py
    - grep -q "test_l_fidelity_in_range" packages/engine/tests/optimizer/property/test_loss_properties.py
    - grep -q "test_total_loss_is_finite" packages/engine/tests/optimizer/property/test_loss_properties.py
    - All property tests pass: uv run pytest packages/engine/tests/optimizer/property/ -x -q exits 0
    - At least 7 property tests
  </acceptance_criteria>
  <done>
    7 hypothesis property tests verify no NaN/Inf and correct value ranges for all loss functions. ROADMAP success criterion 3 (no NaN across random seeds) satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Determinism + memory stability tests</name>
  <files>
    packages/engine/tests/optimizer/determinism/__init__.py
    packages/engine/tests/optimizer/determinism/test_determinism.py
    packages/engine/tests/optimizer/memory/__init__.py
    packages/engine/tests/optimizer/memory/test_rss.py
  </files>
  <read_first>
    packages/engine/src/aerocloud/optimizer/inner_loop.py
    packages/engine/src/aerocloud/utils/determinism.py
    packages/engine/tests/renderer/memory/test_rss.py
    packages/engine/tests/renderer/determinism/test_determinism.py
    packages/engine/tests/optimizer/conftest.py
  </read_first>
  <action>
    **Determinism tests** (`test_determinism.py`):

    1. **test_same_seed_identical_loss_curves**: Run `set_seed(42)` + create renderer + InnerLoop + optimize() 10 times. Collect loss histories. Assert all 10 runs produce byte-identical loss history lists. Use a small config: stage_resolutions=[8], max_epochs=20 for speed.

    2. **test_different_seeds_differ**: Run with seed=42 and seed=99. Assert loss histories are NOT identical (different seeds produce different results).

    3. **test_determinism_across_stages**: Run with seed=42, stage_resolutions=[8, 16], on a 16px SDF. Verify both stages produce identical histories across 3 runs.

    **Memory tests** (`test_rss.py`):

    1. **test_rss_stable_over_100_optimize_steps**: Create a tiny renderer (2 sprites, 8px). Run a single-stage optimization loop (stage_resolutions=[8], max_epochs=5) 100 times. Measure RSS before and after. Assert delta < 50 MiB. Uses `psutil.Process(os.getpid()).memory_info().rss`.

    2. **test_no_tensor_accumulation**: After 50 optimize() calls, verify `torch.cuda.memory_allocated()` (if CUDA) or manual gc.collect() + objgraph-style check that tensor count is bounded.

    3. **test_detach_item_in_loss_history**: Run optimize(). Verify all entries in stage_loss_histories are plain Python floats (not torch.Tensor). `assert all(isinstance(v, float) for stage in result.stage_loss_histories for v in stage)`.

    All tests use small configs (stage_resolutions=[8], max_epochs=5-20) for speed. D-23: CPU only.
  </action>
  <verify>
    <automated>cd /var/www/wordcloud-app-v2/server/aerocloud-engine && uv run pytest packages/engine/tests/optimizer/determinism/ packages/engine/tests/optimizer/memory/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "set_seed" packages/engine/tests/optimizer/determinism/test_determinism.py
    - grep -q "test_same_seed_identical_loss_curves" packages/engine/tests/optimizer/determinism/test_determinism.py
    - grep -q "psutil" packages/engine/tests/optimizer/memory/test_rss.py
    - grep -q "test_rss_stable" packages/engine/tests/optimizer/memory/test_rss.py
    - grep -q "isinstance.*float" packages/engine/tests/optimizer/memory/test_rss.py
    - All determinism + memory tests pass: uv run pytest packages/engine/tests/optimizer/determinism/ packages/engine/tests/optimizer/memory/ -x -q exits 0
    - At least 3 determinism tests and 3 memory tests
  </acceptance_criteria>
  <done>
    Determinism verified: same seed -> identical loss curves over 10 runs (ROADMAP criterion 5). Memory stable: RSS delta < 50 MiB over 100 runs (ROADMAP criterion 4). Loss history entries are plain floats (detach().item() verified).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test inputs | Hypothesis-generated random tensors -- bounded by strategy constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | D (DoS) | hypothesis tests | mitigate | max_examples capped at 200; suppress_health_check prevents runaway |
</threat_model>

<verification>
- `uv run pytest packages/engine/tests/optimizer/ -x -q` -- ALL optimizer tests green (unit + integration + property + determinism + memory)
- Total test count >= 35: `uv run pytest packages/engine/tests/optimizer/ --co -q | tail -1`
- `uv run mypy packages/engine/src/aerocloud/optimizer/ --strict` -- no type errors
- `uv run ruff check packages/engine/src/aerocloud/optimizer/ packages/engine/tests/optimizer/` -- no lint errors
</verification>

<success_criteria>
- >= 7 property tests passing with max_examples >= 50
- >= 3 determinism tests: 10-run identity verified
- >= 3 memory tests: RSS < 50 MiB delta, loss history is float not Tensor
- Total Phase 6 test count >= 35 (D-22 target)
- No NaN/Inf in any property test run (ROADMAP success criterion 3)
- RSS stable (ROADMAP success criterion 4)
- Loss curves identical for same seed (ROADMAP success criterion 5)
</success_criteria>

<output>
After completion, create `.planning/phases/06-inner-loop-v1/06-03-SUMMARY.md`
</output>
