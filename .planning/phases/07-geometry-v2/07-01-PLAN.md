---
phase: 07-geometry-v2
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/engine/src/aerocloud/geometry/mat.py
  - packages/engine/src/aerocloud/geometry/mat_cache.py
  - packages/engine/tests/geometry/unit/test_mat.py
  - packages/engine/tests/geometry/integration/test_mat_integration.py
  - packages/engine/tests/geometry/property/test_mat_property.py
autonomous: true
requirements:
  - GEO2-01
  - GEO2-02

must_haves:
  truths:
    - "Star mask produces more than 1 MAT branch after pruning"
    - "Crescent mask produces more than 1 MAT branch after pruning"
    - "Circle mask with radius=3 default produces exactly 1 branch (simple shape fallback)"
    - "Branch origins are (y,x) tuples with deterministic ordering"
    - "scikit-fmm travel_time output is cast to float32 at the boundary"
    - "MAT cache returns same result for same SDF bytes"
  artifacts:
    - path: "packages/engine/src/aerocloud/geometry/mat.py"
      provides: "extract_mat() public function + MATResult Pydantic model"
      exports:
        - "extract_mat"
        - "MATResult"
        - "MATBranch"
    - path: "packages/engine/src/aerocloud/geometry/mat_cache.py"
      provides: "get_or_build_mat() LRU cache function"
      exports:
        - "get_or_build_mat"
    - path: "packages/engine/tests/geometry/unit/test_mat.py"
      provides: "Unit tests for MAT extraction, pruning, travel_time cast"
    - path: "packages/engine/tests/geometry/integration/test_mat_integration.py"
      provides: "End-to-end MAT on circle/star/crescent .npy fixtures"
    - path: "packages/engine/tests/geometry/property/test_mat_property.py"
      provides: "Hypothesis: random masks produce non-NaN float32 travel_time"
  key_links:
    - from: "mat.py:extract_mat()"
      to: "sdf.py EDT output"
      via: "scipy.ndimage.maximum_filter on sdf > 0 region"
      pattern: "maximum_filter.*sdf"
    - from: "mat.py:extract_mat()"
      to: "skfmm.travel_time()"
      via: "branch orientation from centroid travel time"
      pattern: "skfmm.travel_time"
    - from: "mat_cache.py:get_or_build_mat()"
      to: "sdf_cache.py pattern"
      via: "cachetools.LRUCache + blake3 + RLock"
      pattern: "cachetools.*LRUCache"
---

<objective>
Implement MAT skeleton extraction and pruning as `geometry/mat.py` + `geometry/mat_cache.py`.

Purpose: Multi-Centric placement (Plan 07-04) needs branch origins derived from the medial axis. The MAT is derived from the existing Phase 4 EDT (not re-computed from scratch) with scikit-fmm used only for directional travel_time orientation per D-01.

Output:
- `mat.py` — `extract_mat(sdf, config) -> MATResult` exposing labeled branches + branch origins
- `mat_cache.py` — `get_or_build_mat()` LRU-cached wrapper (cachetools + blake3 + RLock, D-17/D-22 pattern from sdf_cache.py)
- Three test files covering unit, integration, and property tests
</objective>

<execution_context>
@/var/www/wordcloud-app-v2/server/aerocloud-engine/.claude/get-shit-done/workflows/execute-plan.md
@/var/www/wordcloud-app-v2/server/aerocloud-engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-geometry-v2/07-CONTEXT.md
@.planning/phases/07-geometry-v2/07-RESEARCH.md

<interfaces>
<!-- Existing geometry interfaces the executor must use. No codebase exploration needed. -->

From packages/engine/src/aerocloud/geometry/sdf.py (Phase 4):
- SDF dtype: float32, sign convention positive=inside (ADR-0004, D-09), shape (H, W)
- `compute_sdf(mask: BoolMask) -> np.ndarray` is the public entry point

From packages/engine/src/aerocloud/geometry/sdf_cache.py (template for mat_cache.py):
- Pattern: `cachetools.LRUCache(maxsize=..., getsizeof=lambda v: ...)` + `threading.RLock` + blake3 key
- blake3 import: `from blake3 import blake3`

From packages/engine/src/aerocloud/models/base.py:
- `AeroCloudBase(frozen=True, strict=True, extra="forbid")` — base class for all Pydantic models

From packages/engine/src/aerocloud/config.py:
- Pydantic Settings pattern — add `mat_min_branch_radius: float = 3.0` and `mat_cache_max_bytes: int = 128 * 1024 * 1024`

From packages/engine/src/aerocloud/geometry/errors.py:
- `GeometryError` base class — subclass for any new MAT-specific errors if needed

Coordinate convention (D-14): (y, x) is canonical internally. All branch origins are (y, x) tuples.

scikit-fmm CRITICAL (Pitfall 8): `skfmm.travel_time(phi, speed)` requires phi to contain a zero contour. Set `phi[source_y, source_x] = -1.0`, all other inside pixels = `+1.0`, mask outside pixels.

scikit-fmm CRITICAL (Pitfall 1): `skfmm.travel_time()` ALWAYS returns float64. Cast: `np.asarray(tt, dtype=np.float32)`.

Existing test fixture .npy files location: `packages/engine/tests/fixtures/geometry/` — circle.npy, square.npy, crescent.npy fixtures from Phase 4.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — MAT tests (failing)</name>
  <files>
    packages/engine/tests/geometry/unit/test_mat.py
    packages/engine/tests/geometry/integration/test_mat_integration.py
    packages/engine/tests/geometry/property/test_mat_property.py
  </files>

  <read_first>
    - packages/engine/src/aerocloud/geometry/sdf_cache.py (template for cache pattern)
    - packages/engine/src/aerocloud/geometry/sdf.py (SDF API to understand input)
    - packages/engine/tests/geometry/unit/test_sdf.py (test style/fixture patterns)
    - packages/engine/tests/geometry/integration/test_pipeline.py (integration test style)
    - packages/engine/src/aerocloud/config.py (settings pattern)
  </read_first>

  <behavior>
    Unit tests (test_mat.py):
    - test_extract_ridge_points_circle: circle SDF → ridge is a single cluster near center
    - test_extract_ridge_points_filters_noise: SDF with tiny value region below min_branch_radius → filtered out
    - test_binary_dilation_connects_gaps: raw ridge with 1px gap → after dilation iterations=2 → connected
    - test_label_branches_star: star SDF → n_branches > 1 after extract_mat_branches()
    - test_branch_origin_is_yx_tuple: all origins have (y, x) ordering, y < H, x < W
    - test_travel_time_returns_float32: skfmm output cast to float32 at boundary
    - test_travel_time_zero_contour_required: phi without zero level raises ValueError caught by extract_mat
    - test_mat_result_pydantic_valid: MATResult model validates and is frozen

    Integration tests (test_mat_integration.py):
    - test_circle_fixture_single_branch: load circle.npy SDF → extract_mat → n_branches == 1
    - test_star_fixture_multi_branch: load star SDF (create small star fixture or generate analytically) → n_branches > 1
    - test_crescent_fixture_multi_branch: crescent SDF → n_branches > 1
    - test_branch_origins_inside_mask: all branch origins land on sdf > 0 pixels
    - test_fallback_single_branch_origin_is_centroid: 1-branch result → origin near geometric centroid

    Property tests (test_mat_property.py):
    - @given(random bool mask, valid SDF) → extract_mat produces non-NaN float32 travel_time
    - @given(random SDF) → branch origins always have 0 <= y < H and 0 <= x < W
    - @given same SDF twice → same MATResult (determinism property)
  </behavior>

  <action>
    1. Create all three test files. Import `extract_mat`, `MATResult`, `MATBranch` from `aerocloud.geometry.mat` — this import will fail (module does not exist yet) making all tests RED.
    2. Write all test functions as listed in behavior. Use `np.load` for .npy fixtures from `packages/engine/tests/fixtures/geometry/`. For star fixture: if it does not exist, generate analytically in the test (5-arm star as a boolean mask via parametric formula, compute SDF inline using scipy EDT per D-11 formula).
    3. For property tests: use `hypothesis.given` with `hypothesis.strategies.arrays` for mask generation; compute SDF from mask inside the strategy.
    4. Run `uv run pytest packages/engine/tests/geometry/unit/test_mat.py packages/engine/tests/geometry/integration/test_mat_integration.py packages/engine/tests/geometry/property/test_mat_property.py -x 2>&1 | head -40` — confirm ImportError (RED state).
    5. Commit: `test(07-01): add failing MAT tests (RED)`
  </action>

  <verify>
    <automated>uv run pytest packages/engine/tests/geometry/unit/test_mat.py -x 2>&1 | grep -E "ImportError|ModuleNotFoundError|ERROR"</automated>
  </verify>

  <acceptance_criteria>
    - All three test files exist and are syntactically valid Python
    - `uv run pytest packages/engine/tests/geometry/unit/test_mat.py` fails with ImportError (not SyntaxError)
    - Each test file has at least the test functions listed in behavior above
    - RED commit created: `test(07-01): add failing MAT tests (RED)`
  </acceptance_criteria>

  <done>Test files committed, confirmed red via ImportError.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — implement mat.py and mat_cache.py</name>
  <files>
    packages/engine/src/aerocloud/geometry/mat.py
    packages/engine/src/aerocloud/geometry/mat_cache.py
    packages/engine/src/aerocloud/config.py
    packages/engine/src/aerocloud/geometry/__init__.py
  </files>

  <read_first>
    - packages/engine/src/aerocloud/geometry/sdf_cache.py (copy LRU+blake3+RLock pattern exactly)
    - packages/engine/src/aerocloud/models/base.py (AeroCloudBase)
    - packages/engine/src/aerocloud/config.py (add two new settings fields)
    - packages/engine/tests/geometry/unit/test_mat.py (the RED tests — implement to pass these)
  </read_first>

  <behavior>
    mat.py — implement:
    - `MATBranch(AeroCloudBase)`: fields `branch_id: int`, `origin_yx: tuple[int, int]`, `sdf_volume: float`, `pixel_count: int`
    - `MATResult(AeroCloudBase)`: fields `branches: tuple[MATBranch, ...]`, `branch_map: np.ndarray` (int32 (H,W), -1=outside), `travel_time: np.ndarray` (float32 (H,W))
    - `extract_ridge_points(sdf: np.ndarray, min_branch_radius: float) -> np.ndarray`: bool (H,W) mask — local maxima of sdf in inside region via `scipy.ndimage.maximum_filter(sdf, size=3)`, filter `sdf == local_max` and `sdf > min_branch_radius`
    - `extract_mat_branches(sdf: np.ndarray, min_branch_radius: float) -> tuple[np.ndarray, int]`: apply binary_dilation(ridge, iterations=2), then `scipy.ndimage.label(ridge_connected & (sdf > 0))`
    - `compute_branch_travel_times(mask: np.ndarray, centroid_yx: tuple[int,int]) -> np.ndarray`: float32 travel time via skfmm.travel_time — set phi[cy,cx]=-1.0, all inside pixels=+1.0, mask=np.ma.MaskedArray(phi, ~mask), result cast to float32 (D-03, Pitfall 1)
    - `extract_mat(sdf: np.ndarray, min_branch_radius: float = 3.0) -> MATResult`: orchestrates the full pipeline — extract branches, compute centroid, run travel_time, assign branch origins as argmax SDF per labeled component, compute branch_map via nearest-origin Voronoi (D-13), compute sdf_volume per branch
    - Determinism: all tiebreaks use lexicographic (y, x) ordering (D-15 / D-45 carry-forward)

    mat_cache.py — implement:
    - Module-level `_MAT_CACHE: cachetools.LRUCache` with `getsizeof=_mat_result_size` (sum nbytes of branch_map + travel_time arrays)
    - Module-level `_MAT_CACHE_LOCK: threading.RLock`
    - `get_or_build_mat(sdf: np.ndarray, min_branch_radius: float) -> MATResult`: blake3 key `(blake3(sdf.tobytes()).hexdigest(), ("min_branch_radius", min_branch_radius), ("mat_algo_version", 1), sdf.shape)`, cache under RLock, call `extract_mat` on miss

    config.py — add fields:
    - `mat_min_branch_radius: float = 3.0`
    - `mat_cache_max_bytes: int = 128 * 1024 * 1024`

    geometry/__init__.py — export `extract_mat`, `MATResult`, `MATBranch`, `get_or_build_mat`
  </behavior>

  <action>
    1. Read sdf_cache.py fully to copy the exact LRU + blake3 + RLock pattern.
    2. Implement mat.py per behavior above. Key implementation detail for skfmm (Pitfall 8): phi = np.ones(mask.shape, dtype=np.float32), phi[cy, cx] = -1.0, masked_phi = np.ma.MaskedArray(phi, ~mask), tt = skfmm.travel_time(masked_phi, np.ones_like(phi), dx=1.0), then cast.
    3. Implement mat_cache.py per behavior above — 40-50 lines max, identical structure to sdf_cache.py.
    4. Add settings fields to config.py.
    5. Update geometry/__init__.py exports.
    6. Run `uv run pytest packages/engine/tests/geometry/unit/test_mat.py packages/engine/tests/geometry/integration/test_mat_integration.py packages/engine/tests/geometry/property/test_mat_property.py -x` — all GREEN.
    7. Run `uv run mypy --strict packages/engine/src/aerocloud/geometry/mat.py packages/engine/src/aerocloud/geometry/mat_cache.py` — 0 errors.
    8. Run `uv run ruff check packages/engine/src/aerocloud/geometry/mat.py packages/engine/src/aerocloud/geometry/mat_cache.py` — 0 errors.
    9. Commit: `feat(07-01): implement MAT skeleton extraction + cache (GREEN)`
  </action>

  <verify>
    <automated>uv run pytest packages/engine/tests/geometry/unit/test_mat.py packages/engine/tests/geometry/integration/test_mat_integration.py packages/engine/tests/geometry/property/test_mat_property.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - All MAT tests GREEN (`pytest` exit code 0)
    - `uv run mypy --strict packages/engine/src/aerocloud/geometry/mat.py packages/engine/src/aerocloud/geometry/mat_cache.py` exits 0
    - `uv run ruff check packages/engine/src/aerocloud/geometry/mat.py` exits 0
    - Star mask (5-arm, min 50px radius) produces n_branches >= 2 in MATResult
    - `MATResult.travel_time.dtype == np.float32` (D-03 cast verified)
    - Cache hit test passes: calling `get_or_build_mat` twice with same sdf returns identical object reference (cache hit) and both calls produce identical branch counts
    - GREEN commit created
  </acceptance_criteria>

  <done>mat.py + mat_cache.py implemented, all tests green, mypy and ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDF input → mat.py | Float32 array from caller; must validate shape and non-NaN |
| mat_cache.py key | Blake3 of SDF bytes — same SDF bytes with different min_branch_radius must produce different cache key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | mat_cache.py cache key | mitigate | Include min_branch_radius in blake3 key tuple; changing param must bust cache |
| T-07-01-02 | Denial of Service | extract_mat on large SDF | mitigate | scipy.ndimage.maximum_filter is O(N) — acceptable; skfmm is O(N log N) — document max SDF size in config (1024×1024 max for v1) |
| T-07-01-03 | Information Disclosure | debug log of SDF values | accept | SDF values are geometry data, not PII; structlog INFO level only |
| T-07-01-04 | Denial of Service | skfmm on mask with zero unmasked pixels | mitigate | Validate mask has > 0 inside pixels before calling skfmm; raise GeometryError fast |
</threat_model>

<verification>
Full suite verification after both tasks complete:

```bash
uv run pytest packages/engine/tests/geometry/ -v --tb=short 2>&1 | tail -30
uv run mypy --strict packages/engine/src/aerocloud/geometry/mat.py packages/engine/src/aerocloud/geometry/mat_cache.py
uv run ruff check packages/engine/src/aerocloud/geometry/mat.py packages/engine/src/aerocloud/geometry/mat_cache.py
```

Success signals:
- All pre-existing geometry tests still GREEN (no regressions in collision, placement, sdf, glyph)
- New MAT tests all GREEN
- Star fixture: `result.n_branches >= 2`
- Travel time dtype: `float32`
- Cache: second call same SDF returns without recomputing (log shows cache hit)
</verification>

<success_criteria>
1. `extract_mat(star_sdf)` returns MATResult with n_branches >= 2
2. `extract_mat(crescent_sdf)` returns MATResult with n_branches >= 2
3. `extract_mat(circle_sdf)` returns MATResult with n_branches == 1
4. `MATResult.travel_time.dtype == np.float32` (skfmm float64 cast enforced)
5. `get_or_build_mat` called twice with identical SDF returns result from cache (RLock, no recompute)
6. All existing geometry tests still pass (no regressions)
7. mypy --strict and ruff check clean on new files
</success_criteria>

<output>
After completion, create `.planning/phases/07-geometry-v2/07-01-SUMMARY.md`
</output>
