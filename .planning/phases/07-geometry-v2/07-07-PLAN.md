---
phase: 07-geometry-v2
plan: 07
type: execute
wave: 6
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
  - "07-04"
  - "07-05"
  - "07-06"
files_modified:
  - wiki/discussions/2026-04-15-phase-07-3ki-review.md
  - wiki/decisions/2026-04-15-phase-07-geometry-v2-final.md
  - wiki/knowledge/phase-07-geometry-v2-modules.md
autonomous: false
requirements:
  - GEO2-01
  - GEO2-02
  - GEO2-03
  - GEO2-04
  - GEO2-05
  - GEO2-06
  - GEO2-07
  - GEO2-08
  - GEO2-09

must_haves:
  truths:
    - "3-KI review conducted: Claude self-review + Codex adversarial + Gemini research verification"
    - "All GEO2-01..GEO2-09 requirements verified as implemented"
    - "All Phase 7 roadmap success criteria verified green"
    - "Wiki updated: discussions, decisions, knowledge pages for Phase 7"
    - "Jens receives Telegram summary of Phase 7 completion"
  artifacts:
    - path: "wiki/discussions/2026-04-15-phase-07-3ki-review.md"
      provides: "3-KI discussion record with consensus decision"
    - path: "wiki/decisions/2026-04-15-phase-07-geometry-v2-final.md"
      provides: "Final decisions from Phase 7 (deviations, accepted risks)"
    - path: "wiki/knowledge/phase-07-geometry-v2-modules.md"
      provides: "Module documentation for all 5 new geometry modules"
  key_links:
    - from: "3-KI review"
      to: "Phase 7 exit gate"
      via: "All 3 AIs APPROVED (or Jens override documented)"
      pattern: "APPROVED|BLOCKED"
---

<objective>
Conduct the Phase 7 3-KI review (Claude self-review, Codex adversarial review, Gemini research verification), apply any approved fixes, update the wiki, and achieve Phase 7 exit gate.

Purpose: Phase exit gate per ROADMAP.md requires 3-AI peer review approved before Phase 7 is declared complete. This is the human checkpoint where Jens reviews findings and approves/overrides any BLOCKED verdicts.

This plan contains a human checkpoint (`autonomous: false`) after automated validation and 3-KI review execution.
</objective>

<execution_context>
@/var/www/wordcloud-app-v2/server/aerocloud-engine/.claude/get-shit-done/workflows/execute-plan.md
@/var/www/wordcloud-app-v2/server/aerocloud-engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-geometry-v2/07-CONTEXT.md
@wiki/discussions/2026-04-09-phase-04-summary.md
@wiki/discussions/2026-04-14-phase-05-summary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Phase 7 test suite + exit gate validation</name>
  <files>
    packages/engine/tests/
  </files>

  <read_first>
    - .planning/ROADMAP.md §"Phase Exit Gate" (7-point checklist)
    - All 07-0X-SUMMARY.md files (created by previous plans)
  </read_first>

  <action>
    Run the full exit gate checklist:

    1. pytest (all tests):
       `uv run pytest packages/engine/tests/ -v --tb=short 2>&1 | tail -40`
       All tests green. Note final count.

    2. mypy --strict (all engine source):
       `uv run mypy --strict packages/engine/src/aerocloud/ 2>&1 | tail -20`
       0 errors expected.

    3. ruff check + format:
       `uv run ruff check packages/engine/src/aerocloud/ && uv run ruff format --check packages/engine/src/aerocloud/`
       0 errors expected.

    4. Phase 7 roadmap success criteria spot-checks:
       a. Star mask > 1 MAT branch: `uv run python -c "import numpy as np; from scipy.ndimage import distance_transform_edt; from aerocloud.geometry.mat import extract_mat; star = ...; result = extract_mat(sdf); assert result.branch_count > 1"`
       b. Crescent mask > 1 MAT branch: same pattern
       c. SAT rotated collision test: run test_sat_rotated_detects_what_aabb_misses directly
       d. Bitmap pixel-exact test: run test_bitmap_collision_bit_shift_alignment directly
       e. Multi-centric fills concave: run test_star_mask_multi_centric_fills_arms directly

    5. Collect any failing tests, deviations, or warnings. Record in summary.

    6. Send Telegram progress: `bash scripts/telegram-send.sh "Phase 07 Geometry-v2: exit gate checks complete. [N] tests green. Starting 3-KI review."`
  </action>

  <verify>
    <automated>uv run pytest packages/engine/tests/ --tb=short 2>&1 | grep -E "passed|failed|error" | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/engine/tests/` exits 0 (all tests green)
    - `uv run mypy --strict packages/engine/src/aerocloud/` exits 0
    - `uv run ruff check packages/engine/src/aerocloud/` exits 0
    - Phase 7 roadmap success criteria 1-5 verified with concrete commands
    - Telegram sent with test count
  </acceptance_criteria>

  <done>All exit gate checks pass, results recorded, Telegram sent.</done>
</task>

<task type="auto">
  <name>Task 2: 3-KI Review execution</name>
  <files>
    wiki/discussions/2026-04-15-phase-07-3ki-review.md
  </files>

  <read_first>
    - wiki/discussions/2026-04-09-phase-04-summary.md (3-KI review template)
    - .planning/phases/07-geometry-v2/07-CONTEXT.md §decisions (all D-01..D-21)
    - All Phase 7 source files (mat.py, mat_cache.py, quadtree.py, bezier.py, multi_centric.py, collision.py additions)
  </read_first>

  <action>
    Conduct 3-KI review per CLAUDE.md Regel 6:

    **Claude Self-Review (adversarial):**
    Apply CLAUDE.md Anti-Sycophancy Protocol:
    - S-1..S-8 security checks on all new modules
    - L-1..L-8 stability checks (error handling, resource leaks, race conditions, RLock coverage)
    - A-1..A-5 architecture checks (SRP, DRY, coupling, API contract, backward compat)
    Focus areas: BVH cache thread safety, bitmap bit-shift alignment, skfmm float64 cast, multi-centric determinism

    **Codex Review:**
    ```bash
    codex exec --skip-git-repo-check "Review packages/engine/src/aerocloud/geometry/mat.py packages/engine/src/aerocloud/geometry/collision.py packages/engine/src/aerocloud/geometry/bezier.py packages/engine/src/aerocloud/geometry/multi_centric.py for: (1) correctness of bitmap bit-shift alignment in bitmap_collision(), (2) thread safety of BVH LRU cache (RLock coverage), (3) MAT branch determinism with lex tiebreak, (4) any mypy --strict issues missed. Mark APPROVED or BLOCKED with findings."
    ```

    **Gemini Review:**
    ```bash
    gemini -p "Review these Phase 7 Geometry-v2 implementations: MAT via scipy EDT ridge + scikit-fmm travel_time, 5-stage collision hierarchy (AABB→BVH→Quadtree→SAT→Bitmap), Multi-Centric placement, BezierGlyph via cv2.findContours. Check against: RESEARCH.md pitfalls (bit-shift alignment, skfmm float64, MAT dilation), performance implications of Quadtree max_depth=8, correctness of Catmull-Rom to Bezier conversion. Mark APPROVED or BLOCKED."
    ```

    Record all verdicts and findings in `wiki/discussions/2026-04-15-phase-07-3ki-review.md`.
    Follow CLAUDE.md Regel 6: all 3 must APPROVE (or Jens override with documented rationale).

    Apply any APPROVED fixes immediately. BLOCKED verdicts: document for Jens decision.
  </action>

  <verify>
    <automated>ls wiki/discussions/2026-04-15-phase-07-3ki-review.md && grep -E "APPROVED|BLOCKED" wiki/discussions/2026-04-15-phase-07-3ki-review.md | head -10</automated>
  </verify>

  <acceptance_criteria>
    - wiki/discussions/2026-04-15-phase-07-3ki-review.md exists with verdicts from all 3 AIs
    - Any APPROVED fixes applied and all tests still GREEN after fixes
    - BLOCKED items clearly documented with specific file + line references
    - Telegram sent: "Phase 07 3-KI review complete. Verdicts: Claude [X], Codex [X], Gemini [X]. [N] fixes applied. Waiting for Jens decision on [blocked items or 'all approved']"
  </acceptance_criteria>

  <done>3-KI review complete, fixes applied, wiki updated, Telegram sent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 7 Geometry-v2 complete:
    - MAT skeleton extraction + pruning (mat.py + mat_cache.py) — GEO2-01, GEO2-02
    - 5-stage collision hierarchy: BVH (GEO2-04, GEO2-08), Quadtree (GEO2-05), SAT (GEO2-06), Bitmap (GEO2-07)
    - Multi-Centric placement wrapping place_words() per MAT branch — GEO2-03
    - BezierGlyph model + glyph_to_bezier() via cv2 — GEO2-09
    - Renderer dual-mode output (mode='both') + compute_additive_density deleted — D-20/D-21
    - 3-KI review conducted. Verdicts recorded in wiki.
  </what-built>

  <how-to-verify>
    1. Review 3-KI verdicts in wiki/discussions/2026-04-15-phase-07-3ki-review.md
    2. Check any BLOCKED items — decide: approve or override with rationale
    3. Confirm test count meets Phase 4 baseline + new Phase 7 tests
    4. Spot-check: `uv run pytest packages/engine/tests/geometry/ -v --tb=short | grep -E "PASSED|FAILED" | tail -20`
    5. Optional: Run the star mask multi-centric demo:
       `uv run python -c "from aerocloud.geometry.mat import extract_mat; print('MAT ready')" ` — should print MAT ready

    BLOCKED items from 3-KI review (if any) are listed in the Telegram message.
    Override rationale (like Phase 4) should be documented if overriding any BLOCKED verdict.
  </how-to-verify>

  <resume-signal>
    Type "approved" to declare Phase 7 complete.
    Or describe any BLOCKED issues to fix before declaring complete.
    Or "override [item]" with rationale to override any BLOCKED verdict per Phase 4 precedent.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Wiki update + Phase 7 commit + Telegram final</name>
  <files>
    wiki/decisions/2026-04-15-phase-07-geometry-v2-final.md
    wiki/knowledge/phase-07-geometry-v2-modules.md
    wiki/index.md
    wiki/log.md
  </files>

  <read_first>
    - wiki/index.md (to add Phase 7 entries)
    - wiki/log.md (to append Phase 7 log entry)
    - wiki/knowledge/phase-4-known-limits.md (to update with any Phase 7 resolutions)
  </read_first>

  <action>
    1. Create wiki/knowledge/phase-07-geometry-v2-modules.md — document all 5 new modules:
       - mat.py: API, algorithm, known limits
       - mat_cache.py: cache policy, key structure
       - quadtree.py: API, max_depth default reasoning
       - bezier.py: API, cv2 coordinate flip, float64 precision requirement
       - multi_centric.py: API, word partition algorithm, determinism guarantees

    2. Create wiki/decisions/2026-04-15-phase-07-geometry-v2-final.md — record:
       - Any accepted deviations from CONTEXT.md (if any)
       - 3-KI review outcome summary
       - Phase 7 known limits carried to Phase 12

    3. Update wiki/index.md with Phase 7 entries (mat, quadtree, bezier, multi_centric).

    4. Append to wiki/log.md: Phase 07 complete entry.

    5. Git commit all Phase 7 artifacts:
       `git add packages/engine/src/aerocloud/geometry/mat.py packages/engine/src/aerocloud/geometry/mat_cache.py packages/engine/src/aerocloud/geometry/quadtree.py packages/engine/src/aerocloud/geometry/bezier.py packages/engine/src/aerocloud/geometry/multi_centric.py packages/engine/src/aerocloud/geometry/collision.py packages/engine/src/aerocloud/renderer/_renderer.py packages/engine/src/aerocloud/optimizer/loss.py packages/engine/src/aerocloud/optimizer/inner_loop.py wiki/ .planning/`

    6. Send Telegram final: `bash scripts/telegram-send.sh "Phase 07 Geometry-v2: COMPLETE. [N] tests green. MAT, 5-stage collision, Multi-Centric, BezierGlyph, renderer dual-mode all delivered. Ready for Phase 08 Semantic Vector Space."`
  </action>

  <verify>
    <automated>ls wiki/knowledge/phase-07-geometry-v2-modules.md wiki/decisions/2026-04-15-phase-07-geometry-v2-final.md</automated>
  </verify>

  <acceptance_criteria>
    - Both wiki files created with substantive content (not empty stubs)
    - wiki/index.md updated with Phase 7 entries
    - wiki/log.md has Phase 07 complete entry
    - Git commit created with all Phase 7 source + wiki files
    - Telegram final message sent
  </acceptance_criteria>

  <done>Wiki complete, git commit made, Telegram sent. Phase 7 exit gate achieved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 3-KI review findings | Codex + Gemini outputs are advisory; Jens has final say on BLOCKED verdicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Repudiation | 3-KI review verdicts undocumented | mitigate | All verdicts written to wiki/discussions file; git history provides audit trail |
| T-07-07-02 | Denial of Service | Telegram send fails (network issue) | accept | Review continues regardless; Telegram is notification only |
</threat_model>

<verification>
Phase 7 is complete when:
1. `uv run pytest packages/engine/tests/` exits 0
2. `uv run mypy --strict packages/engine/src/aerocloud/` exits 0
3. `uv run ruff check packages/engine/src/aerocloud/` exits 0
4. All roadmap success criteria 1-5 verified
5. 3-KI review documented in wiki with at least one approved verdict
6. Wiki updated
7. Jens approves at checkpoint
</verification>

<success_criteria>
1. All 5 Phase 7 roadmap success criteria verified (star >1 branch, crescent >1 branch, SAT catches rotated collision, bitmap pixel-exact, multi-centric fills concave)
2. 3-KI review completed with documented verdicts
3. All tests GREEN after any 3-KI fixes
4. Wiki: discussions + decisions + knowledge pages created
5. Jens checkpoint approved
6. Telegram final message sent
</success_criteria>

<output>
After completion, create `.planning/phases/07-geometry-v2/07-07-SUMMARY.md`
Session reset: `bash scripts/session-save-and-clear.sh "Phase 07 Geometry-v2 complete"`
</output>
