---
phase: 09-outer-loop-v1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/engine/src/aerocloud/outer_loop/__init__.py
  - packages/engine/src/aerocloud/outer_loop/errors.py
  - packages/engine/src/aerocloud/outer_loop/models.py
  - packages/engine/src/aerocloud/outer_loop/archive.py
  - packages/engine/src/aerocloud/outer_loop/descriptors.py
  - packages/engine/src/aerocloud/config.py
  - packages/engine/pyproject.toml
  - packages/engine/tests/qd/__init__.py
  - packages/engine/tests/qd/conftest.py
  - packages/engine/tests/qd/test_archive.py
  - packages/engine/tests/qd/test_descriptors.py
autonomous: true
requirements:
  - OUTER-01
  - OUTER-02

must_haves:
  truths:
    - "GridArchive accepts 4D behavioral descriptors bounded [0.0, 1.0]"
    - "GaussianEmitter perturbs solutions and feeds them back to the archive"
    - "Behavioral descriptors are computed from post-optimization layout data"
    - "ribs>=0.10.0 is the pinned dependency (not >=0.8.0)"
  artifacts:
    - path: "packages/engine/src/aerocloud/outer_loop/archive.py"
      provides: "ArchiveWrapper around pyribs GridArchive + GaussianEmitter + Scheduler"
      exports: ["ArchiveWrapper"]
    - path: "packages/engine/src/aerocloud/outer_loop/models.py"
      provides: "QualityWeights, QualityMetrics, ArchiveConfig, ReEvalResult Pydantic models"
      exports: ["QualityWeights", "QualityMetrics", "ArchiveConfig", "ReEvalResult"]
    - path: "packages/engine/src/aerocloud/outer_loop/descriptors.py"
      provides: "compute_descriptors() from OptimizationResult + layout data"
      exports: ["compute_descriptors"]
    - path: "packages/engine/src/aerocloud/outer_loop/errors.py"
      provides: "OuterLoopError hierarchy"
      exports: ["OuterLoopError"]
  key_links:
    - from: "packages/engine/src/aerocloud/outer_loop/archive.py"
      to: "ribs.archives.GridArchive"
      via: "pyribs ask/tell interface"
      pattern: "GridArchive.*solution_dim"
    - from: "packages/engine/src/aerocloud/outer_loop/archive.py"
      to: "ribs.emitters.GaussianEmitter"
      via: "emitter feeds scheduler"
      pattern: "GaussianEmitter.*sigma"
---

<objective>
Scaffolding + pyribs GridArchive wrapper + behavioral descriptor computation.

Purpose: Establish the outer_loop package with pyribs MAP-Elites archive (GridArchive + GaussianEmitter), Pydantic models, config fields, test infrastructure, and behavioral descriptor computation from post-optimization layout data.

Output: Working archive wrapper that can add/retrieve solutions, compute behavioral descriptors, and pass the ask/tell cycle. All unit tests green.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-outer-loop-v1/09-CONTEXT.md
@.planning/phases/09-outer-loop-v1/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/engine/src/aerocloud/models/archive.py:
```python
class BehaviorDescriptor(AeroCloudBase):
    shape_fidelity: float = Field(..., ge=0.0, le=1.0)
    rotation_ratio: float = Field(..., ge=0.0, le=1.0)
    symmetry: float = Field(..., ge=0.0, le=1.0)
    semantic_clustering: float = Field(..., ge=0.0, le=1.0)

class MapElitesEntry(AeroCloudBase):
    bin_id: str
    descriptor: BehaviorDescriptor
    fitness: float
    metadata: dict[str, Any]
    created_at: datetime
    updated_at: datetime
```

From packages/engine/src/aerocloud/models/optimizer.py:
```python
class OptimizationResult(AeroCloudBase):
    params: torch.Tensor  # Final (N, 4) [y, x, scale, rotation]
    stage_loss_histories: list[list[float]]
    total_epochs: int
    convergence_flags: list[bool]
    wall_clock_s: float
```

From packages/engine/src/aerocloud/config.py:
```python
class Settings(BaseSettings):
    seed: int = 42
    # ... (no archive fields yet — Task 1 adds them)
```

From packages/engine/src/aerocloud/semantic/embeddings.py:
```python
def encode_surfaces(surfaces: list[str]) -> np.ndarray:
    """Encode word surfaces into (N, 384) float32 BERT embeddings."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffolding + Pydantic models + config + pyribs dependency update</name>
  <files>
    packages/engine/src/aerocloud/outer_loop/__init__.py,
    packages/engine/src/aerocloud/outer_loop/errors.py,
    packages/engine/src/aerocloud/outer_loop/models.py,
    packages/engine/src/aerocloud/config.py,
    packages/engine/pyproject.toml,
    packages/engine/tests/qd/__init__.py,
    packages/engine/tests/qd/conftest.py
  </files>
  <read_first>
    packages/engine/src/aerocloud/config.py,
    packages/engine/src/aerocloud/models/archive.py,
    packages/engine/src/aerocloud/models/optimizer.py,
    packages/engine/pyproject.toml,
    packages/engine/src/aerocloud/models/base.py
  </read_first>
  <behavior>
    - Test 1: QualityWeights fields sum check (7 fields, all float >= 0)
    - Test 2: QualityMetrics all 7 metrics bounded [0.0, 1.0]
    - Test 3: ArchiveConfig bins_per_dim default=10, solution_dim required
    - Test 4: ReEvalResult has fitness_before, fitness_after, drifted bool
    - Test 5: OuterLoopError is subclass of Exception
    - Test 6: Settings has archive_bins_per_dim, novelty_k, flush_every_n, reeval_every_n fields
  </behavior>
  <action>
    1. Update `packages/engine/pyproject.toml`: change `ribs>=0.8.0` to `ribs>=0.10.0` in qd extras (per D-03 correction — pyribs 0.9.0 has breaking API changes). Run `uv pip install -e ".[qd]"` to install.

    2. Create `packages/engine/src/aerocloud/outer_loop/__init__.py` — empty, just docstring.

    3. Create `packages/engine/src/aerocloud/outer_loop/errors.py`:
       - `OuterLoopError(Exception)` base
       - `ArchiveSaturationError(OuterLoopError)`
       - `EliteDriftError(OuterLoopError)`
       - `SolutionDimMismatchError(OuterLoopError)`

    4. Create `packages/engine/src/aerocloud/outer_loop/models.py` with AeroCloudBase pattern (frozen, strict, extra="forbid"):
       - `QualityWeights`: 7 float fields (w_lc, w_lu, w_ss, w_compactness, w_ar, w_ra, w_distortion), all default 1.0/7.0 approx 0.14286. No sum-to-1 validation — use unnormalized like LossWeights (per D-07).
       - `QualityMetrics`: 7 float fields ge=0.0, le=1.0 (layout_coverage, layout_uniformity, space_saving, compactness, aspect_ratio, realized_adjacencies, distortion_score). Method `combined_fitness(weights: QualityWeights) -> float` returning weighted sum.
       - `ArchiveConfig`: bins_per_dim int default=10, solution_dim int required, sigma float default=0.1, batch_size int default=16, max_words int default=200.
       - `ReEvalResult`: bin_id str, fitness_before float, fitness_after float, drifted bool, drift_pct float.

    5. Add to `packages/engine/src/aerocloud/config.py` Settings class:
       - `archive_bins_per_dim: int = Field(default=10, ge=2, le=50, description="Bins per behavioral dimension for MAP-Elites GridArchive (D-02)")`
       - `archive_sigma: float = Field(default=0.1, gt=0.0, description="GaussianEmitter perturbation std dev")`
       - `archive_batch_size: int = Field(default=16, ge=1, description="Solutions per ask() call")`
       - `archive_max_words: int = Field(default=200, ge=10, description="Max word count for fixed solution_dim")`
       - `novelty_k: int = Field(default=15, ge=1, description="k-NN neighbors for novelty search (D-12)")`
       - `flush_every_n: int = Field(default=100, ge=1, description="Batch flush interval to PostgreSQL (D-11)")`
       - `reeval_every_n: int = Field(default=200, ge=1, description="Re-evaluation interval for elites (D-14)")`

    6. Create `packages/engine/tests/qd/__init__.py` (empty) and `packages/engine/tests/qd/conftest.py` with shared fixtures:
       - `archive_config` fixture returning ArchiveConfig(solution_dim=800, bins_per_dim=5) for fast tests
       - `quality_weights` fixture returning default QualityWeights()
       - `sample_quality_metrics` fixture with plausible values
  </action>
  <verify>
    <automated>uv run pytest packages/engine/tests/qd/ -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ribs>=0.10.0" packages/engine/pyproject.toml` returns 1
    - `grep -c "archive_bins_per_dim" packages/engine/src/aerocloud/config.py` returns 1
    - `grep -c "class QualityMetrics" packages/engine/src/aerocloud/outer_loop/models.py` returns 1
    - `grep -c "class OuterLoopError" packages/engine/src/aerocloud/outer_loop/errors.py` returns 1
    - All tests in packages/engine/tests/qd/ pass
  </acceptance_criteria>
  <done>
    outer_loop package exists with models, errors, config fields. ribs>=0.10.0 pinned. Test infra scaffolded. All tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GridArchive wrapper + GaussianEmitter + behavioral descriptor computation</name>
  <files>
    packages/engine/src/aerocloud/outer_loop/archive.py,
    packages/engine/src/aerocloud/outer_loop/descriptors.py,
    packages/engine/tests/qd/test_archive.py,
    packages/engine/tests/qd/test_descriptors.py
  </files>
  <read_first>
    packages/engine/src/aerocloud/outer_loop/models.py,
    packages/engine/src/aerocloud/outer_loop/errors.py,
    packages/engine/src/aerocloud/models/archive.py,
    packages/engine/src/aerocloud/models/optimizer.py,
    packages/engine/src/aerocloud/config.py,
    .planning/phases/09-outer-loop-v1/09-RESEARCH.md (Pattern 1: pyribs Ask-Tell Loop)
  </read_first>
  <behavior>
    - Test 1: ArchiveWrapper creates GridArchive with correct dims/ranges
    - Test 2: ask() returns (batch_size, solution_dim) numpy array
    - Test 3: tell() with objectives and measures updates archive
    - Test 4: coverage property returns float [0.0, 1.0]
    - Test 5: capacity property returns bins_per_dim^4
    - Test 6: solution_dim = max_words * 4 (per D-16, params are (N,4) flattened)
    - Test 7: compute_descriptors returns BehaviorDescriptor with all fields in [0.0, 1.0]
    - Test 8: shape_fidelity = LC metric value (per RESEARCH BD computation table)
    - Test 9: rotation_ratio = fraction of words with |theta| > pi/4
    - Test 10: descriptors are clamped to [0.0, 1.0] (Pitfall 5)
  </behavior>
  <action>
    1. Create `packages/engine/src/aerocloud/outer_loop/archive.py`:
       - `class ArchiveWrapper`: wraps pyribs GridArchive + GaussianEmitter + Scheduler.
       - `__init__(self, config: ArchiveConfig, seed: int = 42)`:
         - `solution_dim = config.max_words * 4` (D-16: (N,4) flattened)
         - Create `GridArchive(solution_dim=solution_dim, dims=[config.bins_per_dim]*4, ranges=[(0.0, 1.0)]*4, seed=seed)` per D-01, D-02
         - Create `GaussianEmitter(archive=self._archive, sigma=config.sigma, batch_size=config.batch_size, seed=seed)` — per D-03 correction, use GaussianEmitter NOT MapElitesBaselineEmitter (which does not exist in pyribs)
         - Create `Scheduler(archive=self._archive, emitters=[self._emitter])`
         - Use structlog for logging
       - `ask() -> np.ndarray`: delegates to scheduler.ask(), returns (batch_size, solution_dim)
       - `tell(objectives: np.ndarray, measures: np.ndarray)`: clamp measures to [0.0, 1.0] via np.clip (Pitfall 5), then scheduler.tell(objectives, measures)
       - `@property coverage -> float`: `len(self._archive) / self._archive.cells`
       - `@property capacity -> int`: `self._archive.cells`
       - `@property num_elites -> int`: `len(self._archive)`
       - `retrieve(measures: np.ndarray)`: delegates to archive.retrieve()
       - `data() -> dict`: returns archive.data() for iteration
       - `best_elite() -> dict`: archive.best_elite()

    2. Create `packages/engine/src/aerocloud/outer_loop/descriptors.py`:
       - `compute_descriptors(params: torch.Tensor, lc_metric: float, embeddings: np.ndarray, positions_yx: np.ndarray, sdf_mask: np.ndarray) -> BehaviorDescriptor`:
         - `shape_fidelity = lc_metric` (per RESEARCH: shape_fidelity = LC metric)
         - `rotation_ratio = float(np.mean(np.abs(params[:n_words, 3].detach().cpu().numpy()) > (np.pi / 4)))` — fraction of words with |theta| > 45 degrees
         - `symmetry`: compute horizontal reflection score. Project word positions onto horizontal axis. Mirror left-side positions. Compare density distributions via 1D histogram correlation (simplified Wasserstein: `1 - |mean(left_x) - mean(right_x)| / max_x`). Clamp to [0,1].
         - `semantic_clustering`: compute mean pairwise spatial distance / embedding distance ratio, then invert: `1 - clamp(distortion / max_distortion, 0, 1)`. Use scipy.spatial.distance.cdist for both spatial and embedding distances.
         - All 4 values clamped to [0.0, 1.0] via `max(0.0, min(1.0, val))` before constructing BehaviorDescriptor.
         - `n_words` parameter for actual word count (solution may be padded to max_words per Pitfall 3).

    3. Write tests in `test_archive.py` and `test_descriptors.py` following TDD red-green.
  </action>
  <verify>
    <automated>uv run pytest packages/engine/tests/qd/test_archive.py packages/engine/tests/qd/test_descriptors.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GaussianEmitter" packages/engine/src/aerocloud/outer_loop/archive.py` returns >= 1
    - `grep -c "MapElitesBaselineEmitter" packages/engine/src/aerocloud/outer_loop/archive.py` returns 0 (MUST NOT exist)
    - `grep -c "np.clip" packages/engine/src/aerocloud/outer_loop/archive.py` returns >= 1 (Pitfall 5 clamp)
    - `grep -c "def compute_descriptors" packages/engine/src/aerocloud/outer_loop/descriptors.py` returns 1
    - All tests pass
  </acceptance_criteria>
  <done>
    ArchiveWrapper wraps pyribs GridArchive + GaussianEmitter (per D-03 correction). Ask/tell cycle works. Behavioral descriptors computed post-optimization. All values clamped [0,1]. Tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| params tensor | Untrusted optimizer output — may contain NaN, Inf, or out-of-range values |
| pyribs archive | External library managing elite storage — trust but verify API contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T (Tampering) | params tensor input | mitigate | Clamp all descriptor values to [0.0, 1.0] before archive.add (Pitfall 5) |
| T-09-02 | D (DoS) | solution_dim mismatch | mitigate | Validate solution shape matches config.max_words * 4 at ArchiveWrapper boundary |
</threat_model>

<verification>
- `uv run pytest packages/engine/tests/qd/ -x -q` — all tests green
- `uv run mypy packages/engine/src/aerocloud/outer_loop/ --strict` — clean
- `uv run ruff check packages/engine/src/aerocloud/outer_loop/` — clean
- `grep -r "MapElitesBaselineEmitter" packages/engine/` returns 0 matches
</verification>

<success_criteria>
- pyribs GridArchive + GaussianEmitter wrapper works with ask/tell cycle
- Behavioral descriptors computed from post-optimization layout data (D-17)
- All 4 BD dimensions bounded [0.0, 1.0] with clamping
- ribs>=0.10.0 pinned in pyproject.toml
- Config fields added to Settings
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/09-outer-loop-v1/09-01-SUMMARY.md`
</output>
