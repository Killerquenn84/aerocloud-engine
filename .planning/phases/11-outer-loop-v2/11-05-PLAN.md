---
phase: 11-outer-loop-v2
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified:
  - wiki/code/outer-loop-v2-bop-emitter.md
  - wiki/code/outer-loop-v2-cqd.md
  - wiki/code/outer-loop-v2-pareto.md
  - wiki/knowledge/phase-11-decisions.md
  - wiki/discussions/2026-04-21-phase-11-review.md
autonomous: false
requirements:
  - OUTER2-01
  - OUTER2-02
  - OUTER2-03
  - OUTER2-04
  - OUTER2-05
  - OUTER2-06
  - OUTER2-07
  - OUTER2-08
  - OUTER2-09

must_haves:
  truths:
    - "3-KI review approved all Phase 11 code"
    - "Wiki documents all new modules and decisions"
    - "Phase exit gates all green (pytest + mypy + ruff)"
  artifacts:
    - path: "wiki/code/outer-loop-v2-bop-emitter.md"
      provides: "Documentation for CappedBOPEmitter"
    - path: "wiki/code/outer-loop-v2-cqd.md"
      provides: "Documentation for CQD metric"
    - path: "wiki/code/outer-loop-v2-pareto.md"
      provides: "Documentation for Pareto module"
    - path: "wiki/knowledge/phase-11-decisions.md"
      provides: "Phase 11 architectural decisions"
  key_links:
    - from: "wiki/discussions/2026-04-21-phase-11-review.md"
      to: "packages/engine/src/aerocloud/outer_loop/"
      via: "3-KI review references all new modules"
      pattern: "APPROVED"
---

<objective>
3-KI Review + Wiki Update + Phase Exit Gate for Outer Loop-v2.

Purpose: Complete the phase with 3-AI peer review (Claude + Codex + Gemini), update the wiki with all new modules and decisions, and verify all phase exit gates pass.

Output: 3-KI review consensus, wiki pages for all new modules, phase exit verification.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-outer-loop-v2/11-CONTEXT.md
@.planning/phases/11-outer-loop-v2/11-01-SUMMARY.md
@.planning/phases/11-outer-loop-v2/11-02-SUMMARY.md
@.planning/phases/11-outer-loop-v2/11-03-SUMMARY.md
@.planning/phases/11-outer-loop-v2/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3-KI Review (Claude self-review + Codex + Gemini)</name>
  <files>
    wiki/discussions/2026-04-21-phase-11-review.md
  </files>
  <action>
    Per CLAUDE.md Rule 6 (3-Daumen-Prinzip):

    1. Claude Code Self-Review: Review all 4 new/modified source files critically:
       - bop_emitter.py: CappedBOPEmitter correctness, _dataset trimming safety
       - archive.py: BOP/Gaussian dual-mode, extra_fields construction, tell() backwards compat
       - cqd.py: Vectorized omega, BallTree NN, smoothing, empty archive guard
       - pareto.py: pymoo NDS framing (negate for minimization), HV ref_point, slider interpolation

    2. Run Codex review: `codex exec --skip-git-repo-check "Review the Phase 11 outer_loop code in packages/engine/src/aerocloud/outer_loop/ for security (S-1..S-8), stability (L-1..L-8), and architecture (A-1..A-5) per the Anti-Sycophancy protocol. Files: bop_emitter.py, archive.py (changes), cqd.py, pareto.py."`

    3. Run Gemini review: `gemini -p "Review the Phase 11 outer_loop code in packages/engine/src/aerocloud/outer_loop/ for performance and security. Check: CappedBOPEmitter history trimming, CQD vectorization correctness, pymoo HV ref_point orientation, Pareto-Slider interpolation edge cases. Files: bop_emitter.py, archive.py, cqd.py, pareto.py."`

    4. Run team_discuss for consensus: all 3 must APPROVE or document blockers.

    5. Write consensus to wiki/discussions/2026-04-21-phase-11-review.md.

    6. Apply any fixes identified in the review.

    IMPORTANT: Per CLAUDE.md Anti-Sycophancy Protocol — actively search for weaknesses. No "looks good" without concrete justification.
  </action>
  <verify>
    <automated>cd /var/www/wordcloud-app-v2/server/aerocloud-engine && test -f wiki/discussions/2026-04-21-phase-11-review.md && grep -c "APPROVED" wiki/discussions/2026-04-21-phase-11-review.md</automated>
  </verify>
  <done>3-KI consensus documented. All 3 AIs APPROVED (or blockers resolved). Review file exists with APPROVED count >= 3.</done>
</task>

<task type="auto">
  <name>Task 2: Wiki update + Phase exit gates</name>
  <files>
    wiki/code/outer-loop-v2-bop-emitter.md,
    wiki/code/outer-loop-v2-cqd.md,
    wiki/code/outer-loop-v2-pareto.md,
    wiki/knowledge/phase-11-decisions.md
  </files>
  <action>
    1. Create wiki/code/outer-loop-v2-bop-emitter.md:
       - CappedBOPEmitter class, constructor params, history_cap logic
       - Why GPyTorch fallback (D-02): not installed, sklearn GP scales to 700 evals with cap=200
       - Sobol overhead at d=800: ~4s per ask(), acceptable for nightly

    2. Create wiki/code/outer-loop-v2-cqd.md:
       - compute_cqd() algorithm, vectorized omega, BallTree NN
       - CQD equation (D-05), Monte-Carlo (D-06), theta-sweep (D-07)
       - delta_max = sqrt(4) = 2.0 for [0,1]^4
       - Performance: < 25ms for 500 elites

    3. Create wiki/code/outer-loop-v2-pareto.md:
       - extract_pareto_front() with pymoo NDS
       - compute_cqd_hv() cell-by-cell HV aggregation
       - pareto_slider() interpolation + nearest-neighbor lookup
       - Pitfall: negate objectives for pymoo minimization framing

    4. Create wiki/knowledge/phase-11-decisions.md:
       - D-01 through D-14 implementation notes
       - GPyTorch NOT installed -> CappedBOPEmitter fallback
       - extra_fields for layout_coverage + space_saving (breaking change to tell())
       - pymoo HV ref_point orientation

    5. Run phase exit gates:
       ```bash
       cd /var/www/wordcloud-app-v2/server/aerocloud-engine
       python -m pytest packages/engine/tests/qd/ -v
       python -m mypy packages/engine/src/aerocloud/outer_loop/ --strict
       ruff check packages/engine/src/aerocloud/outer_loop/
       ruff format --check packages/engine/src/aerocloud/outer_loop/
       ```

    6. Update wiki/index.md with links to new pages.
  </action>
  <verify>
    <automated>cd /var/www/wordcloud-app-v2/server/aerocloud-engine && python -m pytest packages/engine/tests/qd/ -v && python -m mypy packages/engine/src/aerocloud/outer_loop/ --strict && ruff check packages/engine/src/aerocloud/outer_loop/ && ruff format --check packages/engine/src/aerocloud/outer_loop/</automated>
  </verify>
  <done>4 wiki pages created. Phase exit gates all green: pytest green, mypy --strict clean, ruff clean. wiki/index.md updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Jens verifies Phase 11 completion</name>
  <files>wiki/discussions/2026-04-21-phase-11-review.md</files>
  <action>Present Phase 11 results to Jens for final approval. All automated work is complete — this is verification only.</action>
  <what-built>Complete Phase 11 Outer Loop-v2: BOP-Elites emitter, CQD metric, Pareto-Front + HV, Pareto-Slider, 3-KI review, wiki updates.</what-built>
  <how-to-verify>
    1. Review 3-KI consensus at wiki/discussions/2026-04-21-phase-11-review.md — all 3 APPROVED?
    2. Run: `cd /var/www/wordcloud-app-v2/server/aerocloud-engine && python -m pytest packages/engine/tests/qd/ -v` — all green?
    3. Check wiki pages exist: wiki/code/outer-loop-v2-*.md and wiki/knowledge/phase-11-decisions.md
    4. Verify SC1-SC5 from ROADMAP success criteria are covered by tests
  </how-to-verify>
  <verify>
    <automated>cd /var/www/wordcloud-app-v2/server/aerocloud-engine && python -m pytest packages/engine/tests/qd/ -v</automated>
  </verify>
  <done>Jens approved Phase 11 completion.</done>
  <resume-signal>Type "approved" to mark Phase 11 complete, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Review + documentation phase, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | S (Spoofing) | 3-KI review | mitigate | Anti-sycophancy protocol: each AI must independently find weaknesses before approving |
</threat_model>

<verification>
```bash
cd /var/www/wordcloud-app-v2/server/aerocloud-engine
python -m pytest packages/engine/tests/qd/ -v
python -m mypy packages/engine/src/aerocloud/outer_loop/ --strict
ruff check packages/engine/src/aerocloud/outer_loop/
ruff format --check packages/engine/src/aerocloud/outer_loop/
```
</verification>

<success_criteria>
1. 3-KI review: all 3 AIs APPROVED
2. Wiki pages complete for all 3 new modules + decisions
3. Phase exit gates: pytest green, mypy --strict clean, ruff clean
4. Jens approves phase completion
</success_criteria>

<output>
After completion, create `.planning/phases/11-outer-loop-v2/11-05-SUMMARY.md`
</output>
