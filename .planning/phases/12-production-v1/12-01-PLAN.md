---
phase: 12-production-v1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/engine/pyproject.toml
  - apps/api/pyproject.toml
  - packages/engine/src/aerocloud/export/__init__.py
  - packages/engine/src/aerocloud/export/seam_carving.py
  - packages/engine/src/aerocloud/export/png_export.py
  - packages/engine/src/aerocloud/export/boolean_union.py
  - packages/engine/tests/export/__init__.py
  - packages/engine/tests/export/unit/__init__.py
  - packages/engine/tests/export/unit/test_seam_carving.py
  - packages/engine/tests/export/unit/test_png_export.py
  - packages/engine/tests/export/unit/test_boolean_union.py
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-05
  - PROD-06

must_haves:
  truths:
    - "Seam carving energy map builds from word positions and weights"
    - "DP seam finder returns optimal vertical seam in O(w*h)"
    - "PNG export converts torch tensor to valid PNG bytes"
    - "Boolean union merges overlapping BezierGlyph polygons via shapely"
  artifacts:
    - path: "packages/engine/src/aerocloud/export/seam_carving.py"
      provides: "SeamCarver with build_energy_map() and find_vertical_seam()"
      exports: ["build_energy_map", "find_vertical_seam", "remove_vertical_seam"]
    - path: "packages/engine/src/aerocloud/export/png_export.py"
      provides: "tensor-to-PNG export"
      exports: ["export_png"]
    - path: "packages/engine/src/aerocloud/export/boolean_union.py"
      provides: "Bezier polygon boolean union"
      exports: ["union_glyphs"]
  key_links:
    - from: "boolean_union.py"
      to: "shapely.ops.unary_union"
      via: "BezierGlyph tessellation → Shapely Polygon → unary_union"
      pattern: "unary_union"
    - from: "seam_carving.py"
      to: "scipy.ndimage.gaussian_filter"
      via: "Energy map computation with Gaussian weighting"
      pattern: "build_energy_map"
---

<objective>
Install new dependencies (shapely, slowapi, prometheus stack, sse-starlette, otel-otlp, scikit-image) and implement the core export modules: seam carving (PROD-01/02), PNG export (PROD-05), and boolean union (PROD-06).

Purpose: These export modules are foundational — SVG/PDF export (Plan 02) and the FastAPI render endpoint (Plan 05) consume them.
Output: Working seam carving, PNG export, boolean union with full TDD test coverage.
</objective>

<execution_context>
@/var/www/wordcloud-app-v2/server/aerocloud-engine/.claude/get-shit-done/workflows/execute-plan.md
@/var/www/wordcloud-app-v2/server/aerocloud-engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-production-v1/12-CONTEXT.md
@.planning/phases/12-production-v1/12-RESEARCH.md

@packages/engine/pyproject.toml
@apps/api/pyproject.toml
@packages/engine/src/aerocloud/geometry/bezier.py
@packages/engine/src/aerocloud/models/api.py

<interfaces>
From packages/engine/src/aerocloud/geometry/bezier.py:
```python
class BezierCurve(AeroCloudBase):
    p0: tuple[float, float]  # (y, x) start
    p1: tuple[float, float]  # (y, x) control 1
    p2: tuple[float, float]  # (y, x) control 2
    p3: tuple[float, float]  # (y, x) end

class BezierGlyph(AeroCloudBase):
    font_family: str
    size_pt: int
    codepoint: int
    contours: tuple[tuple[BezierCurve, ...], ...]
    tolerance: float
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies + seam carving module (PROD-01, PROD-02)</name>
  <files>
    packages/engine/pyproject.toml,
    apps/api/pyproject.toml,
    packages/engine/src/aerocloud/export/__init__.py,
    packages/engine/src/aerocloud/export/seam_carving.py,
    packages/engine/tests/export/__init__.py,
    packages/engine/tests/export/unit/__init__.py,
    packages/engine/tests/export/unit/test_seam_carving.py
  </files>
  <behavior>
    - build_energy_map(100, 100, [(50,50)], [1.0], sigma=15.0) returns (100,100) float64 array with peak near (50,50)
    - build_energy_map energy values are strictly positive where words exist
    - find_vertical_seam on uniform energy returns any valid column-index array of length h
    - find_vertical_seam on energy with single low-cost column returns seam through that column
    - find_vertical_seam returns int32 array with len == canvas height
    - find_vertical_seam seam values are within [0, width-1] and adjacent rows differ by at most 1
    - remove_vertical_seam on (10,10) image returns (10,9) image
    - remove_vertical_seam removes the correct column per row (pixel identity check)
  </behavior>
  <action>
    1. Add new dependencies per D-06 and research. In packages/engine/pyproject.toml add `shapely>=2.1.2` to a `[production]` optional group. In apps/api/pyproject.toml add: `slowapi>=0.1.9`, `prometheus-client>=0.25.0`, `prometheus-fastapi-instrumentator>=7.1.0`, `opentelemetry-exporter-otlp>=1.41.0`, `sse-starlette>=3.3.4`. Add `scikit-image>=0.26.0` to dev dependencies. Run `uv sync --all-extras` to install.

    2. Create `packages/engine/src/aerocloud/export/__init__.py` with exports: `build_energy_map`, `find_vertical_seam`, `remove_vertical_seam`, `export_png`, `union_glyphs`.

    3. Write tests FIRST in `test_seam_carving.py` per behavior spec above.

    4. Implement `seam_carving.py` per D-02:
       - `build_energy_map(canvas_h, canvas_w, word_positions, word_weights, sigma=15.0) -> np.ndarray` — Vectorized: use np.mgrid + broadcasting, Gaussian E(x,y) = sum(w_i * exp(-dist^2 / (2*sigma^2))).
       - `find_vertical_seam(energy: np.ndarray) -> np.ndarray` — DP O(w*h). Vectorize inner loop using np.minimum on shifted row arrays (no Python for-loop over columns). Backtrack from min of last row.
       - `remove_vertical_seam(image: np.ndarray, seam: np.ndarray) -> np.ndarray` — Remove one pixel per row at seam column indices. Works on 2D or 3D arrays.
       
    All functions: pure numpy, no torch dependency. Type-annotated, docstrings with references to PROD-01/PROD-02.
  </action>
  <verify>
    <automated>cd /var/www/wordcloud-app-v2/server/aerocloud-engine && uv run pytest packages/engine/tests/export/unit/test_seam_carving.py -x -v</automated>
  </verify>
  <done>
    - build_energy_map produces correct Gaussian-weighted energy on test fixtures
    - find_vertical_seam returns valid optimal seam (adjacent-column constraint, correct length)
    - remove_vertical_seam shrinks image by exactly 1 column
    - All tests green, mypy clean on new files
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PNG export + boolean union modules (PROD-05, PROD-06)</name>
  <files>
    packages/engine/src/aerocloud/export/png_export.py,
    packages/engine/src/aerocloud/export/boolean_union.py,
    packages/engine/tests/export/unit/test_png_export.py,
    packages/engine/tests/export/unit/test_boolean_union.py
  </files>
  <behavior>
    - export_png on (3, 64, 64) float32 tensor in [0,1] returns valid PNG bytes (starts with PNG magic bytes)
    - export_png on (1, 64, 64) grayscale tensor returns valid PNG bytes
    - export_png roundtrip: tensor -> PNG -> PIL.Image -> numpy matches original within 1/255
    - bezier_curve_to_points tessellates a straight-line BezierCurve into n+1 collinear (x,y) points
    - glyph_to_polygon converts a simple square BezierGlyph to a Shapely Polygon with correct area
    - union_glyphs on two overlapping square glyphs returns a polygon with area < sum of individual areas
    - union_glyphs on non-overlapping glyphs returns MultiPolygon with area == sum of individual areas
    - union_glyphs on empty list returns empty geometry
  </behavior>
  <action>
    1. Write tests FIRST per behavior spec.

    2. Implement `png_export.py` per D-05:
       - `export_png(tensor: torch.Tensor, mode: str = "RGB") -> bytes` — Accepts (C, H, W) or (1, H, W) or (H, W) float32 tensor in [0.0, 1.0]. Clamp, scale to [0, 255], .byte().cpu().numpy(), transpose to (H, W, C) for RGB, `Image.fromarray()`, save to BytesIO as PNG, return bytes.
       - Handle grayscale (mode="L") and RGB (mode="RGB") explicitly.

    3. Implement `boolean_union.py` per D-06:
       - `bezier_curve_to_points(curve: BezierCurve, n: int = 20) -> list[tuple[float, float]]` — De Casteljau tessellation, flip (y,x) → (x,y) for Shapely.
       - `glyph_to_polygon(glyph: BezierGlyph) -> Polygon | None` — First contour = outer ring, subsequent contours = holes. Uses bezier_curve_to_points.
       - `union_glyphs(glyphs: list[BezierGlyph]) -> BaseGeometry` — `shapely.ops.unary_union` on polygons. Returns union geometry (Polygon or MultiPolygon). Per research anti-pattern: do NOT convert back to BezierGlyph.

    4. Update `export/__init__.py` to export all public names.
  </action>
  <verify>
    <automated>cd /var/www/wordcloud-app-v2/server/aerocloud-engine && uv run pytest packages/engine/tests/export/unit/test_png_export.py packages/engine/tests/export/unit/test_boolean_union.py -x -v</automated>
  </verify>
  <done>
    - PNG export produces valid PNG bytes from float32 tensors (RGB and grayscale)
    - Boolean union correctly merges overlapping BezierGlyph polygons
    - Non-overlapping glyphs union preserves total area
    - All tests green, mypy clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export input | Tensors and BezierGlyphs from internal pipeline — trusted |
| shapely | External C library (GEOS) processing tessellated coordinates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | D (DoS) | seam_carving.py | mitigate | Validate canvas_h/canvas_w <= 4096 at function entry; reject oversized inputs |
| T-12-01-02 | T (Tampering) | png_export.py | accept | Input tensors are internal pipeline output, not user-supplied directly |
| T-12-01-03 | D (DoS) | boolean_union.py | mitigate | Limit tessellation point count (n <= 50) to prevent memory explosion on degenerate curves |
</threat_model>

<verification>
```bash
cd /var/www/wordcloud-app-v2/server/aerocloud-engine
uv run pytest packages/engine/tests/export/ -x -v
uv run mypy packages/engine/src/aerocloud/export/ --strict
```
</verification>

<success_criteria>
- Seam carving energy map + DP seam + remove seam all tested and working
- PNG export roundtrip within 1/255 tolerance
- Boolean union merges overlapping Bezier polygons correctly
- All new dependencies installed and importable
- mypy strict clean on all export modules
</success_criteria>

<output>
After completion, create `.planning/phases/12-production-v1/12-01-SUMMARY.md`
</output>
