# Nightly Research Summary: 2026-04-16 to 2026-04-18

> 15 research files across 3 nights. Prepared for team_discuss review.

---

## 2026-04-16 (5 topics)

### Adam Optimizer Hyperparameters
New iAdam (look-ahead mechanism) and OptiRoulette (meta-optimizer switching between AdamW/SGD) papers. Key finding: beta1=beta2 "stability hack" transforms Adam into SNR optimizer for noisy gradients. PyTorch 3.0 nightly auto-fuses Adam kernels for H100/B200 (30-60% speedup). 8-bit AdamW now mandatory default for LLMs.

### Seam Carving Word Cloud Whitespace
"Hybrid Energy Maps for Multi-Resolution Seam Carving" paper (arXiv 15.04.2026) shows 15% better shape fidelity with neural attention maps. OpenCV 6.1.2 hotfix for memory leaks in large accumulate ops. Sharp 0.45.0 pre-release changes alpha channel default behavior. CVE-2026-1182 (Critical) in svg.path library allows RCE via manipulated path data.

### Sinkhorn-Knopp Optimal Transport
Breakthrough convergence proof: O(log(1/epsilon)) iterations independent of dimension (arXiv:2604.03787). "Sinkhorn Flow as Mirror Flow" enables new stabilization techniques. POT v0.9.5 improves batch GPU parallelization. OTT-JAX stabilizes Multi-Marginal Sinkhorn. VEM Attack warning for Federated Learning using Gumbel-Sinkhorn.

### spaCy Transformer Models German English
CVE-2026-32274 (Critical) in bundled black library. Python 3.9 support dropped (minimum 3.10). Pydantic v2 migration completed for spacy-llm/confection/thinc (breaking change). spacy-transformers v1.3.7+ adds Gemma 4 and ModernBERT support. Label mismatch between DE (PER) and EN (PERSON) models noted.

### structlog OpenTelemetry Tracing
OTel declarative YAML/JSON configuration now stable. Span Events API deprecated in favor of Logs/Events API. LoggingHandler deprecated for opentelemetry-instrumentation-logging. CVE-2026-29181 (High) in otel-python-contrib baggage header extraction (DoS). Llama Stack v0.7.1 migrated fully to structlog.

---

## 2026-04-17 (5 topics)

### Bitmap Pixel-Exact Collision
"Neural-Bitmasking" paper (Eurographics 2026, ETH Zurich) predicts collision likelihood before bitmask comparison, reducing CPU load by 40%. "SDF-assisted Bitmap Intersection" uses SDFs to skip pixel checks when objects are far apart. Rust crate fast-pixel-collide v2.4.1 switches to AVX-512 SIMD by default (breaking change for older hardware). CVE-2026-11409 integer overflow in LibImageDetect bitmap processing.

### Medial Axis Transform scikit-fmm
"Neural Medial Axis Fields" paper integrates MAT into NeRF for 40% artifact reduction in thin-wall geometries. scikit-fmm 2026.4.1 drops Python 3.9, adds optimized CUDA kernels for distance fields. Comp-Geom-Toolkit v4.2 patches precision error on near-parallel edges. NASA recommends order=2 (second-order accurate) as default for MAT calculations.

### PyTorch3D Soft-Rasterization
PyTorch3D officially in maintenance mode (no new features). Breaking change in Camera API (get_projection_transform exceptions). CVE-2026-24747 (Critical) RCE in weights_only unpickler via crafted .pth files. DiffSoup introduces stochastic opacity masking (eliminates SoftRas blurring). DiffBMP framework uses Gaussian blur for bitmap primitive position gradients.

### safetensors Model Checkpoints Security
No new CVEs or breaking changes. Hugging Face enhanced header validation against DoS via malformed metadata. Best practice: SHA-256 checksums before VRAM loading (not just at download). Theoretical "adversarial tensors" research (arXiv:2604.0821) discusses logic bombs in pure data formats. Safetensors remains gold standard over .ckpt/.bin (pickle).

### spaCy Transformer Models German English
spacy-llm v0.7.4 completes Pydantic v2 migration (breaking change for validators). spacy-transformers v3.7 adds ModernBERT support (8k token window). Python 3.9 EOL confirmed. CVE-2026-32274 still active. New paper warns about Zipf distribution biases in synthetic fine-tuning data. Alpha releases of en_core_web_modernbert/de_core_news_modernbert show 15% speed improvement.

---

## 2026-04-18 (5 topics)

### CQD Quality-Diversity Metric Kent 2022
No new publications or breaking changes. CQD remains gold standard for QD algorithm comparison. QDAX v0.4.x improved stochastic CQD for noisy fitness evaluation. Current research trend: "Cross-Resolution CQD" for comparing metrics across different grid resolutions. Pyribs recommends rank-based archive for stable CQD trajectories.

### CUDA 12 PyTorch Compatibility Matrix
No major releases. Stable base: PyTorch 2.7.0 + CUDA 12.8. FlashAttention 3.1.2 patches memory leak on Blackwell (H200/B200). Triton 3.2 JIT caching issue under CUDA 12.8 — set TORCHINDUCTOR_CACHE_DIR explicitly. NVIDIA paper on kernel-level fusion for sparse-dense operators. Recommended: Python 3.13, NVIDIA driver >= 575.xx.

### Seam Carving Word Cloud Whitespace
Google Looker 26.4 GA with improved word cloud collision checking. G'MIC 3.7.5 updates seamcarve documentation for artifact avoidance. Research trend: semantic whitespace as thematic distance indicator (not just waste to remove). Seam carving forensics: over-optimized clouds may be flagged as "manipulated images" by forensic tools. Voronoi-hybrid models emerging as alternative to pure seam carving.

### Signed Distance Field Meijster EDT
SDF-Filleting for additive manufacturing using double-offset algorithm. Parallel-Meijster evolution: partitioned recursive strategy for GPU optimization of phase 2. Adobe Substance 3D Designer 16.0 adds procedural SDF node system. MuJoCo 3.7.0 introduces SDF collision primitives (breakthrough for non-convex collision). CVE-2026-23865 (Critical) FreeType integer overflow in SDF rendering mode — update to FreeType 2.14.2+. msdfgen 1.12 breaking change in glyph coordinate system.

### SVG Sanitization Security CVE
CVE-2026-25133 (October CMS) SVG sanitization bypass via regex manipulation. CVE-2026-6216 (DbGate) XSS in icon string handler. CVE-2026-22610 (Angular 19) script injection bypass in SVGs. DOMPurify v3.4.0 fixes critical jsdom bypass + prototype pollution (urgent update for Node.js backends). is-svg v6.0.0 stricter validation (breaking change for legacy SVGs). OX Security report identifies malicious SVGs as zero-click vector for AI IDE prompt injection.
